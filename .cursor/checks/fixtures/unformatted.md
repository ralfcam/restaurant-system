# Unformatted fixture

- this line has trailing spaces   
- and this paragraph is one extremely long markdown line that prettier would wrap at the default print width because it far exceeds eighty columns in a single run-on sentence without breaks

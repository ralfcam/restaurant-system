# Security findings (open)

- [ ] Staff-path startsWith is segment-unaware · `lib/supabase/proxy.ts:35-36` (`STAFF_PATHS`) · `/administrator` would be treated as `/admin` and redirected by the staff gate; same class as AC-19, different owner (`staff-authorization.md` SA-2) · med · (found: tdd/res-50_locale_mw_session_cookies_a3f1c8e2/planning)
- [ ] Public INSERT RLS is unconstrained · `supabase/migrations/00000000000000_baseline.sql:114-115` reservations INSERT policy · `TO public` / `WITH CHECK (true)` plus table-wide `GRANT INSERT` lets a Data API client set `status` / `table_label` / `completed_at` unless the booking trigger rejects them · med · (found: tdd/res-45_review_email_schema_cron_7f3a91c2/C3/refactor) (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C3/green)
- [ ] Hidden review-email `enabled` field is not a gated control · `app/admin/marketing/review-email-settings-form.tsx` · SA-10 chrome lists Switch/Textarea/Input/Save; the hidden `enabled` field can still post if someone bypasses disabled widgets (server SA-7/SA-8 still blocks) · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C332-4/red)
- [ ] Review-email `handleSubmit` does not bail when `!isSuperAdmin` · `app/admin/marketing/review-email-settings-form.tsx` · Disabled chrome can be re-enabled in the DOM and still POST; server `requireSuperAdminUser` still rejects · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C332-4/green)
- [ ] `review_email_sends` uses `REVOKE ALL … FROM anon, authenticated` and omits `PUBLIC` · `supabase/migrations/00000000000000_baseline.sql` (review_email_sends grants) · leftover default privileges can survive a role-only revoke; sibling table is outside C3–C7 · low · (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C3/red)
- [ ] Data API roles still keep default `REFERENCES`/`TRIGGER`/`TRUNCATE` on sibling catalog tables that were not moved to `REVOKE ALL` · `supabase/migrations/20260825140000_operating_windows_privilege.sql:15` (`operating_windows`) and `:44` (`restaurant_settings`) · public-read tables still use `GRANT SELECT` + `REVOKE INSERT, UPDATE, DELETE` · low · (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C4/refactor)
- [ ] Guest Data API can still `SELECT` 86'd rows · `supabase/migrations/00000000000000_baseline.sql:177` (`FOR SELECT TO public USING (true)` plus SELECT grants) · AC-1 hides unavailable items in the guest UI, but PostgREST still returns `available = false` · med · (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C5/refactor)
- [ ] Realtime still publishes `tables` / `table_merges` · `supabase/migrations/20260818180000_floor_tables.sql` publication block · guest Data API SELECT is denied; confirm linked Realtime cannot subscribe without table privilege · low · (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C7/refactor)

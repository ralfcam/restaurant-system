# Security findings (open)

- [ ] C8 still does not pin the SELECT allowlist string · tests/unit/analytics/staff-page.test.ts vs app/actions/analytics.ts:24-25 · thenables ignore .select(); select("*") would still pass C8; a live * would still load guest columns into data before the map · med · (found: tdd/analytics_tdd_wave_44867fc0/C8/green)
- [ ] RA-2 is application-only · supabase/migrations/00000000000000_baseline.sql service_role FOR ALL + GRANT ALL on reservations / tables / status_events · createServiceClient() can still mutate; a later .update in this action would succeed at the DB · med · (found: tdd/analytics_tdd_wave_44867fc0/C9/refactor)
- [ ] Unauthorized tab mutations return `undefined`, same as the authorized no-op · `app/actions/menu.ts` · Sibling item CRUD returns `{ error: "Unauthorized." }`; a client that only checks `error` will treat a rejected write as success. C5/C6 do not pin this. · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C1/refactor)
- [ ] Staff list collapses auth failure, query error, and empty catalog to `[]` · `app/actions/menu.ts:285,292-294` · After C4, a missing table or expired session looks like “no tabs” · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C3/refactor)
- [ ] No title/id validation on staff tab actions · `app/actions/menu.ts:292-321` · Once C3+ persist through service_role, unsanitized titles/`id`s land in `menus` (same gap as item CRUD; trust-boundary, not in C1 test). · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C1/refactor)
- [ ] Unvalidated PII write · `app/actions/guest-profiles.ts:32` · `guest_name`/`phone` are written as submitted (empty, oversized, untrimmed); siblings trim/bound contact fields · med · (found: tdd/res-104_guest_profiles_f8c2e1a0/GP-10/refactor)
- [ ] `select("*")` plus `...row` serializes extra reservation columns · `app/actions/guest-profiles.ts:15` → `lib/guest-profiles.ts:37` · history objects include `conf_code` / `table_label` / `id` / `created_at`; a later client-invoked action or chrome would ship them · med · (found: tdd/res-104_guest_profiles_f8c2e1a0/GP-8/refactor)
- [ ] Normalized href can open an empty ficha for mixed-case stored emails · `lib/guest-profiles.ts:5` → `app/actions/guest-profiles.ts:16` · booking create only trims; the control navigates to `/admin/customers/{lowercased}` and the reader `.eq("email", key)` misses `Ada@Ex.com` · med · (found: tdd/res-104_guest_profiles_f8c2e1a0/GP-9/refactor)

# Security findings (open)

- [ ] anon/authenticated still have TRUNCATE/REFERENCES/TRIGGER/MAINTAIN (Dxtm) on reservations · live relacl anon=aDxtm, authenticated=aDxtm · RES-PRIV revokes SELECT/UPDATE/DELETE only; TRUNCATE bypasses RLS (PostgREST does not expose it) · low · (seen: /triage 2026-08-28) (found: tdd/reazed-308_privileges_be131e14/C1/refactor)
- [ ] anon/authenticated still have TRUNCATE/REFERENCES/TRIGGER/MAINTAIN (Dxtm) on blocked_dates · table ACL after GRANT SELECT + REVOKE INSERT/UPDATE/DELETE · sibling of reservations Dxtm; TRUNCATE bypasses RLS (PostgREST does not expose it) · low · (seen: /triage 2026-08-28) (found: tdd/reazed-308_privileges_be131e14/C2/refactor)
- [ ] anon/authenticated still have TRUNCATE/REFERENCES/TRIGGER/MAINTAIN (Dxtm) on menu_items · table ACL after GRANT SELECT + REVOKE INSERT/UPDATE/DELETE · sibling of reservations/blocked_dates Dxtm; TRUNCATE bypasses RLS (PostgREST does not expose it) · low · (seen: /triage 2026-08-28) (found: tdd/reazed-308_privileges_be131e14/C3/refactor)
- [ ] occupancy columns inherit authenticated FOR ALL · `supabase/migrations/00000000000000_baseline.sql:514` · any authenticated client can PATCH `occupancy_duration_minutes` / `safety_buffer_minutes` via PostgREST; C5 gates the Server Action only · low · (seen: /triage 2026-08-28) (found: tdd/reazed-309_occupancy_buffer_1862f023/C4/green)
- [ ] Unauthenticated occupancy/buffer getters return defaults instead of throwing · `app/actions/branding.ts:393,408` · same fail-open as `getSlotIntervalMinutes`; anyone who can POST the Server Action learns 90/15 · low · (seen: /triage 2026-08-28) (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/refactor)
- [ ] SECURITY DEFINER trigger stays in `public` · `supabase/migrations/00000000000000_baseline.sql:186` · C8 added owner-bypass SELECT on `reservations`/`tables`/`restaurant_settings`; moving DEFINER out of the exposed schema is a redesign · low · (seen: /triage 2026-08-28) (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/refactor)

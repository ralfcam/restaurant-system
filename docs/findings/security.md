# Security findings (open)

- [ ] Authenticated `FOR ALL` on `restaurant_settings` · baseline RLS · Direct PostgREST writes can bypass later action-layer PV-10 checks; same as other settings, not anon (GRANT SELECT only) · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C1/refactor)
- [ ] Draft Maps URL stored unsanitized · `app/actions/marketing.ts` `saveReviewEmailSettings` (enabled-off upsert) · Incomplete drafts persist arbitrary `mapsUrl` (`http:`, `javascript:`); enable cannot turn on with those values, but a later UI `href` of the stored draft without an `https:` check would be XSS · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C2/green)
- [ ] Action payload is trusted after staff auth · `app/actions/marketing.ts:14` · no runtime parse; `copy: null` from a crafted action POST throws on `.trim()` instead of returning the PV-10 error · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C2/refactor)
- [ ] Table-level `GRANT INSERT` will let anon persist `email` · `supabase/migrations/00000000000000_baseline.sql:110` / `:135` · table-level `GRANT INSERT` + `WITH CHECK (true)` does not strip the new column; a crafted PostgREST insert can store PII this spec does not collect · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C10/red)
- [ ] Bearer check is not constant-time · `app/api/cron/review-email/route.ts:7` · `!==` can leak secret length/timing under a local attacker; Vercel Cron still matches this header shape · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C9/green)
- [ ] getAuthUser does not re-check the staff claim · app/actions/auth.ts:12 · staff pages read this after the proxy; a skipped middleware still treats a guest session as signed-in staff chrome context · low · (seen: /triage 2026-08-30) (found: REAZED-310/planning)
- [ ] Auth callback defaults next to /admin with no staff claim · app/auth/callback/route.ts:7-13 · SA-3 only pins the password login page; OAuth/code users can still be sent at /admin before the SA-2 bounce · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C1/red)
- [ ] JWT staff claim is not re-checked for freshness at the proxy · lib/supabase/proxy.ts:31-40 · `getUser()` trusts current JWT `app_metadata`; a revoked `role` can remain staff until the access token refreshes · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C2/refactor)
- [ ] Non-staff session remains after rejected staff login · app/auth/login/page.tsx:35-37 · `signInWithPassword` still sets the browser session; the page only withholds `/admin` and does not `signOut`. Staff routes/actions stay gated (C1/C2), but guest chrome can see a leftover authenticated session · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C3/green)
- [ ] Email confirmations stay off independently of signup · supabase/config.toml `[auth.email] enable_confirmations = false` · SA-4 does not pin this; if hosted/local signup is re-enabled, new accounts can sign in without confirming email · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C4/red)
- [ ] EMAIL_RE allows `<>"'` in local/domain · `lib/reservations/validation.ts:23` · BW-14’s HTML escape list is name/date/time/party/`conf_code` only; if C7 interpolates `to` into html, markup in the address is XSS · low · (found: mailing_automations_triple_2347e1d5.plan/C3/refactor)

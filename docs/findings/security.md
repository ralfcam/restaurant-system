# Security findings (open)

- [ ] Hidden review-email `enabled` field is not a gated control · `app/admin/marketing/review-email-settings-form.tsx` · SA-10 chrome lists Switch/Textarea/Input/Save; the hidden `enabled` field can still post if someone bypasses disabled widgets (server SA-7/SA-8 still blocks) · low · (seen: /triage 2026-09-11) (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C332-4/red)
- [ ] Review-email `handleSubmit` does not bail when `!isSuperAdmin` · `app/admin/marketing/review-email-settings-form.tsx` · Disabled chrome can be re-enabled in the DOM and still POST; server `requireSuperAdminUser` still rejects · low · (seen: /triage 2026-09-11) (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C332-4/green)
- [ ] `review_email_sends` uses `REVOKE ALL … FROM anon, authenticated` and omits `PUBLIC` · `supabase/migrations/00000000000000_baseline.sql` (review_email_sends grants) · leftover default privileges can survive a role-only revoke; sibling table is outside C3–C7 · low · (seen: /triage 2026-09-11) (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C3/red)
- [ ] Data API roles still keep default `REFERENCES`/`TRIGGER`/`TRUNCATE` on sibling catalog tables that were not moved to `REVOKE ALL` · `supabase/migrations/20260825140000_operating_windows_privilege.sql:15` (`operating_windows`) and `:44` (`restaurant_settings`) · public-read tables still use `GRANT SELECT` + `REVOKE INSERT, UPDATE, DELETE` · low · (seen: /triage 2026-09-11) (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C4/refactor)
- [ ] Realtime still publishes `tables` / `table_merges` · `supabase/migrations/20260818180000_floor_tables.sql` publication block · guest Data API SELECT is denied; confirm linked Realtime cannot subscribe without table privilege · low · (seen: /triage 2026-09-11) (found: tdd/res-42_sibling_privilege_lock_6f2a8c1d/RES42-C7/refactor)
- [ ] cookie-less anon factory also unfenced · `lib/supabase/client-server.ts` · same comment-then-import shape as `service.ts` but uses `NEXT_PUBLIC_SUPABASE_ANON_KEY`; a Client Component can still import it; SA-11 only requires the service-role fence · low · (seen: /triage 2026-09-11) (found: tdd/res-37_service_boundary_ffc7ec3c/RES37-C1/red)
- [ ] Database-level guest-note cap deferred · `supabase/migrations/00000000000000_baseline.sql` / `replace_operating_windows` · direct trusted service-role writes can bypass application validation; operator selected app/server rejection without a DB CHECK for this run · low · (seen: /triage 2026-09-11) (found: tdd/res-41_guest_note_cap_49176a07/planning/seed)
- [ ] `service_role` still has EXECUTE on the trigger function · live `proacl` `service_role=X/postgres` after local reset · spec only closes `PUBLIC`/`anon`/`authenticated`; a service-key RPC of a `RETURNS TRIGGER` function is leftover privileged surface, not a guest hole · low · (seen: /triage 2026-09-11) (found: tdd/res_47_grant_hardening_a330bfed/C1/refactor)

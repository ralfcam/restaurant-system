# Security findings (open)

- [ ] Unauthorized tab mutations return `undefined`, same as the authorized no-op · `app/actions/menu.ts` · Sibling item CRUD returns `{ error: "Unauthorized." }`; a client that only checks `error` will treat a rejected write as success. C5/C6 do not pin this. · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C1/refactor)
- [ ] Staff list collapses auth failure, query error, and empty catalog to `[]` · `app/actions/menu.ts:285,292-294` · After C4, a missing table or expired session looks like “no tabs” · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C3/refactor)
- [ ] No title/id validation on staff tab actions · `app/actions/menu.ts:292-321` · Once C3+ persist through service_role, unsanitized titles/`id`s land in `menus` (same gap as item CRUD; trust-boundary, not in C1 test). · med · (found: tdd/res-70_admin_menu_tabs_c3a91f2b/C1/refactor)
- [ ] Unvalidated PII write · `app/actions/guest-profiles.ts:32` · `guest_name`/`phone` are written as submitted (empty, oversized, untrimmed); siblings trim/bound contact fields · med · (found: tdd/res-104_guest_profiles_f8c2e1a0/GP-10/refactor)
- [ ] `select("*")` plus `...row` serializes extra reservation columns · `app/actions/guest-profiles.ts:15` → `lib/guest-profiles.ts:37` · history objects include `conf_code` / `table_label` / `id` / `created_at`; a later client-invoked action or chrome would ship them · med · (found: tdd/res-104_guest_profiles_f8c2e1a0/GP-8/refactor)
- [ ] Absolute-path normalize is checkout-name-hardcoded · .cursor/hooks/lib/tdd-guard-policy.mjs:260 · normalize() only strips /restaurant-system/, so a Cursor Write of /workspace/lib/foo.ts would not match lib/ and would not deny while armed · med · (found: tdd/pr118_cr_gtd1_guard_e1b8c4a2/C1/red)
- [ ] Orchestrator can edit .cursor/hooks scripts while armed · .cursor/hooks/lib/tdd-guard-policy.mjs:22 · PROTECTED_PREFIXES covers product hooks/ but not the guard scripts; only the state-file escape is specified · med · (found: tdd/pr118_cr_gtd1_guard_e1b8c4a2/C1/refactor)
- [ ] Prefix-only work-order path is not normalized · `.cursor/hooks/lib/coderabbit-pr-policy.mjs:95` · `startsWith(".cursor/plans/")` would treat `.cursor/plans/../lib/billing/foo.ts` as process-meta; GitHub paths are usually repo-relative without `..`, but the gate would skip a crafted path · low · (found: tdd/pr118_cr_gcr3_plan_th_a422e8f1/C1/refactor)
- [ ] Reservation SELECT fail-open skips BW-18 · app/actions/reservations.ts:834 · on query error every generated time is available:true so a staff allowlist/slot cap is ignored in the widget · med · (found: tdd/res-71_slot_service_covers_a8c1e2f4/C1/refactor)
- [ ] Non-staff sign-in keeps a session · `app/auth/login/page.tsx:37-48` · `signInWithPassword` succeeds before the staff check, and the unauthorized branch returns without `signOut` · med · (found: tdd/res-77_full_french_ui_830c8b81/C12/refactor)
- [ ] Callback next is concatenated onto the origin · `app/auth/callback/route.ts:7-13` · `next` is appended to `origin` with no same-origin path check · med · (found: tdd/res-77_full_french_ui_830c8b81/C12/refactor)

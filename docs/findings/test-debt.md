# Test-debt findings (open)

- [ ] unit setup no-ops `server-only` · `tests/unit/setup.ts:3` · Vitest will not throw on a missing `import "server-only"` except where a source-read test exists; C6 `booking-confirmation.ts` has no PV-9-style pin, so dropping the import would not fail C6 · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C1/refactor)
- [ ] No unit coverage for `claimError` skip · `tests/unit/marketing/review-email-send.test.ts` overlapping case only returns empty `data` · A PostgREST error on the CAS is logged and skipped in source but never asserted · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C2/refactor)
- [ ] Whitespace-only email is not asserted · `tests/unit/reservations/validation.test.ts` email describe · BW-13 requires trimmed non-empty email; named cases only cover `undefined` and `not-an-email` · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C3/red)
- [ ] Domain-without-dot email (`user@localhost`) is not asserted · `tests/unit/reservations/validation.test.ts` · BW-13 requires `local@domain` with a `.` in the domain; `rejects an invalid email` uses `not-an-email`, so a mere `@` check could green it · med · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C3/red)
- [ ] `basePayload` still casts and redeclares `email?: string` · `tests/unit/reservations/validation.test.ts:11-23` · leftover from Red before `ReservationPayload` had `email` · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C3/refactor)
- [ ] No unit coverage of createReservation blocked-date / closed / outside-hours early returns · `app/actions/reservations.ts` hours/blocked branches · those paths never reach insert and are not in C5–C7 · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C4/red)
- [ ] persist test still casts the payload · `tests/unit/reservations/create-reservation.test.ts:75` · leftover Red cast; now hides a type error if `email` is dropped from the action type · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C4/refactor)
- [ ] C5 never mounts the form · `tests/unit/reservation-widget/guest-email.test.ts:16` · source-read pins JSX `required` / payload `email`, not Base UI forwarding or native constraint validation · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C5/refactor)
- [ ] BW-14 P0001 path is not asserted to skip send · `tests/unit/reservations/create-reservation.test.ts` · C6 only pins validation-error; a send that runs before the P0001 return would still pass · med · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C6/red)
- [ ] Review-email PV-7 does not pin `&` / `<` escaping · `tests/unit/marketing/review-email-send.test.ts:369` · copy/URL have no special characters, so dropping `escapeHtml` would still pass · med · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C7/red)
- [ ] C7 test still casts `sendBookingConfirmation` for a second argument · `tests/unit/marketing/booking-confirmation.test.ts:6` · the cast can hide later signature drift now that the source declares `mailer?` · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C7/red)
- [ ] Escape coverage stops at `&` / `<` · `tests/unit/marketing/booking-confirmation.test.ts` · `>` / `"` / `'` in the name are escaped but unasserted · low · (seen: /triage 2026-09-01) (found: mailing_automations_triple_2347e1d5.plan/C7/green)
- [ ] POS/KDS skip is decision-only · `tests/unit/i18n/middleware-scope.test.ts:39-44` · Admin sibling drives `middleware()` so `updateSession` still runs on skip; this `it()` never hits the composition root, so a future `middleware.ts` that ignores `resolveLocaleRoutingDecision` for `/pos`/`/kds` would still pass · low · (found: REAZED-280/C1/refactor)
- [ ] Staff gate does not assert empty `app_metadata` · tests/unit/auth/require-staff.test.ts · sibling covers unauthenticated + user_metadata spoof + staff, not empty app_metadata; C2 only adds super_admin-implies-staff · low · (found: tdd/reazed-326_super_admin_permission_model/C1/red)
- [ ] Combined staff gate cases under a null-only name · tests/unit/auth/require-staff.test.ts:19 · The existing `it()` mixes unauthenticated, `user_metadata` spoof, and a successful `"staff"` claim, but is named only for the null path · low · (found: tdd/reazed-326_super_admin_permission_model/C2/red)
- [ ] Staff route/proxy not covered for hierarchy · lib/supabase/proxy.ts / app/auth/login/page.tsx (callers of `isStaffUser`) · SA-2 says a `super_admin` session satisfies staff chrome, but C2 only asserts `requireStaffUser`; those surfaces now inherit the widened check with no unit case · low · (found: tdd/reazed-326_super_admin_permission_model/C2/green)
- [ ] Mock factory dropped `requireStaffUser` · tests/unit/branding/actions.test.ts:23 · Source still imports both gates; getter tests assert `mocks.requireStaffUser` was not called, but that hoisted fn is no longer wired into the mock, so the assertion is vacuous · med · (found: tdd/reazed-326_super_admin_permission_model/C3/refactor)
- [ ] Booking-config writes have no unauthenticated-caller case · tests/unit/floor/slot-interval.test.ts / occupancy-settings.test.ts · C3 asserts both unauthenticated and staff-only; C4 only added staff-only · low · (found: tdd/reazed-326_super_admin_permission_model/C4/red)
- [ ] Read-path gate not isolated from the super-admin mock · tests/unit/floor/occupancy-settings.test.ts · `getSafetyBufferMinutes` with both mocks returning users; a mistaken swap of `loadRestaurantBookingSettings` to `requireSuperAdminUser` would still pass these suites · med · (found: tdd/reazed-326_super_admin_permission_model/C4/refactor)
- [ ] `getRestaurantInfoBar` has no unit coverage · app/actions/restaurant-info.ts · public read + fallbacks to `RESTAURANT.address`/`phone` are untested; C5 is write-gate only · low · (found: tdd/reazed-326_super_admin_permission_model/C5/red)
- [ ] Unused `requireStaffUser` mock leftover in C6 test · tests/unit/marketing/review-email-settings.test.ts:4 · Red kept both mocks; source no longer calls `requireStaffUser` · low · (found: tdd/reazed-326_super_admin_permission_model/C6/refactor)
- [ ] Shared seed-SQL parser duplicated · `tests/unit/auth/seed-staff-claim.test.ts` vs `seed-super-admin-claim.test.ts` · same VALUES splitter / column zip; C1 added `authUsersInsertForId` but older `staffAuthUsersInsert` (`:21-34`) still takes the first `auth.users` INSERT — reordering would break SA-5 coverage without failing C1 · med · (found: tdd/reazed-326_super_admin_permission_model/C7/refactor) (found: tdd/seed_users_email_f5f7f0e6.plan.md/C1/red)
- [ ] Existing C7 empty-email `if` skip · `tests/unit/auth/seed-super-admin-claim.test.ts:127-132` · leftover vacuous branch (`if (seededEmail !== "")`); C2 now pins the value, so a future empty-string regression would still pass that older `it` and rely solely on C2 · low · (found: tdd/seed_users_email_f5f7f0e6.plan.md/C1/red)
- [ ] Pairwise uniqueness only vs staff · `tests/unit/auth/seed-super-admin-claim.test.ts` C2 `it` · SA-9 requires all seed `auth.users.email` values pairwise distinct; C2 only compares super-admin vs staff; a third seed user with a colliding email would not fail this test · low · (found: tdd/seed_users_email_f5f7f0e6.plan.md/C2/red)
- [ ] FIX-mode broader unit timeout · `tests/unit/dev-toolchain/lint-toolchain.test.ts:35` · 5s timeout spinning ESLint over gitignored supabase temp trees; `pnpm lint` itself passed · med · (found: tdd/seed_users_email_f5f7f0e6.plan.md/C2/refactor)

# Test-debt findings (open)

- [ ] Privilege `toContain` matches SQL comments · `tests/unit/scheduling/schema.test.ts:93-104` and C1 EARLY-PRIV (`:106-119`) · a `--` comment quoting `GRANT ALL ON TABLE <t> TO service_role` would still pass without an executable GRANT · low · (seen: /triage 2026-08-27) (found: tdd/reazed-297_sibling_grants_097b94f3/C1/red, C1/green)
- [ ] Action suite never pins the 90-minute due boundary · `tests/unit/reservations/auto-assign-action.test.ts:82-96` · booked `18:00` at mocked now `18:00` is due under both T−15 and T−90, so the action test would not catch a caller reintroducing a 15-minute lead · low · (seen: /triage 2026-08-27) (found: tdd/reazed-307_fp2_lead_8387cb6a/C1/red)
- [ ] C2 helper pin is a source-text `/90/` on raw TSX · `tests/unit/floor/schema.test.ts:67-76` · interpolating `{DEFAULT_EXPECTED_MINUTES}` in the helper would drop the literal `90` from the captured JSX and fail this test even when runtime copy is correct · low · (seen: /triage 2026-08-27) (found: tdd/reazed-307_fp2_lead_8387cb6a/C2/green)
- [ ] C1 SQL contract does not pin GRANT SELECT … TO authenticated · tests/integration/reservations/public-privileges.integ.test.ts:24 / :108 · booking-rules AC-5 forbids SELECT grants to both anon and authenticated; `GRANT SELECT ON TABLE reservations TO authenticated` (or `TO authenticated, anon`) would not match `GRANT_SELECT_ANON` · med · (found: tdd/reazed-308_privileges_be131e14/C1/red)
- [ ] C2 live API only asserts anon INSERT denial, not UPDATE/DELETE · tests/integration/reservations/public-privileges.integ.test.ts:156 · REVOKE string is pinned in SQL; a live UPDATE/DELETE grant-leak would not fail this it · low · (found: tdd/reazed-308_privileges_be131e14/C2/refactor)
- [ ] C3 live API only asserts anon INSERT denial, not UPDATE/DELETE · tests/integration/reservations/public-privileges.integ.test.ts:200-210 · REVOKE string is pinned in SQL; a live UPDATE/DELETE grant-leak would not fail this it · low · (found: tdd/reazed-308_privileges_be131e14/C3/refactor)
- [ ] Safety buffer 0 and 60 (BW-11 inclusive bounds) untested · `tests/unit/reservations/operating-hours.test.ts` C3 · a falsy `value || 15` helper or a clamp that rejects 0/60 would still pass 15/7/NaN/61; C2 already treats buffer `0` as a real next-bookable input · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C3/red)
- [ ] slot_interval pin joins every migration instead of naming chefs-picks · `tests/unit/branding/schema.test.ts:38-46` · `ADD COLUMN IF NOT EXISTS slot_interval_minutes` can pass from any leftover `.sql` while `20260823130000_restaurant_info_and_chefs_picks.sql` drifts · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C4/red)
- [ ] C4 pin is SQL-text regex, not executed DDL · `tests/unit/branding/schema.test.ts:50` · `7` / `241` / `61` never fail an INSERT this criterion; live CHECK enforcement waits on C8 / a DB-backed schema test · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C4/refactor)
- [ ] Overlapping partial holds are not summed · `tests/unit/reservations/available-slots.test.ts` occupancy-window describe · C6 only seeds one occupying row; two overlapping parties (e.g. 19:00×20 + 19:15×20 vs capacity 40) could still leave 20:30 offered if Green special-cases a single full-floor hold instead of summing overlapping covers · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C6/red)
- [ ] Cover tests seed `19:00`, not production `19:00:00` · `tests/unit/reservations/available-slots.test.ts:151` · dropping `normalizeTime` would still pass C6 · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C6/refactor)
- [ ] `from()` thenable ignores `.in("status")` / `.eq("date")` · `tests/unit/reservations/available-slots.test.ts` `thenable` proxy · query filters are no-ops, so a later status/date test only proves JS-side counting of whatever the stub returns, not the Supabase filter · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C6/red)
- [ ] C7 only pins 20:30 after release, not an in-window slot · `tests/unit/reservations/available-slots.test.ts` early-release `it` · Green could still occupy `[19:00, 20:30)` for `completed`/`cancelled`/`no_show` (drop only the buffer) and pass; BW-10 says those statuses do not occupy at all · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C7/red)
- [ ] Mixed occupying + released rows on one date are untested · `tests/unit/reservations/available-slots.test.ts` occupancy-window describe · statuses are stubbed one-at-a-time, so a whole-query shortcut that drops every cover when any `completed` row is present would still pass C7 while zeroing a sibling `seated` hold · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C7/red)
- [ ] occupancy-window `stubFrom` hardcodes 15-minute interval · `tests/unit/reservations/available-slots.test.ts` occupancy-window describe · C10 had to duplicate the whole `from()` mock to use 30; later interval variants will keep copying · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C10/red)
- [ ] Full-hold in-window slots besides 20:30/21:00 unpinned on the 30-min grid · `tests/unit/reservations/available-slots.test.ts:219` · C10 (and C6 Case A) never assert `19:00`/`20:00` unavailable for a capacity-filling 19:00 hold; a special-case that only flips `20:30` would still pass both grid pins · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C10/refactor)
- [ ] Trigger does not pin `seated` as occupying · `tests/integration/reservations/occupancy-window.integ.test.ts` · C8 seeds only `confirmed`; a Green that occupies confirmed-only would pass while a `seated` hold still accepts 20:30 · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/red)
- [ ] Trigger early-release for `cancelled` is untested · `tests/integration/reservations/occupancy-window.integ.test.ts` · BW-10 lists `cancelled`; this run only completes the seed, so a Green that releases on `completed` but keeps `cancelled` occupying would still pass · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/red)
- [ ] Overlapping partial holds are unsummed at the trigger · `tests/integration/reservations/occupancy-window.integ.test.ts` · one full-floor row; two overlapping parties that together exceed `sum(tables.seats)` could still insert if Green special-cases a single full hold · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/red)
- [ ] Slot-interval suite does not pin RSC wiring · `tests/unit/floor/slot-interval.test.ts` · `FloorPlan`’s `initialSlotInterval` default can hide a dropped `getSlotIntervalMinutes()` fetch on `app/admin/floor/page.tsx` · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/red)
- [ ] Floor settings persist tests do not assert `requireStaffUser` before upsert · `tests/unit/floor/slot-interval.test.ts` and `tests/unit/floor/occupancy-settings.test.ts` · an unauthenticated write path would still green · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/red)
- [ ] C5 never mounts `FloorPlan` · `tests/unit/floor/occupancy-settings.test.ts` · regex-scans chrome and calls actions directly; a broken `MinutesStepperButtons` `onClick` would still pass · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/refactor)
- [ ] Widget regex cannot prove occupancy comes from `restaurant_settings` · `tests/unit/reservation-widget/segment-groups.test.ts` C9 / `components/site/reservation-widget.tsx` SlotCard · a local `occupancyDurationMinutes = 90` (or a mis-copied payload field) would satisfy the occupancy-named second-arg pin without reading the BW-11 singleton · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C9/red)
- [ ] `SlotAvailability` cannot carry occupancy · `tests/unit/reservations/available-slots.test.ts` `toEqual({ time, available })` · C9 Green used a second settings read instead of the plan’s “field on the slot payload” because C6 pins exact `{ time, available }` · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C9/green)
- [ ] FP-11 physical-row / merge-member counting is untested · `tests/unit/floor/dashboard-occupancy.test.ts` anti-seed fixture has no merge members · a merge-collapsed count would still satisfy 7/9 · med · (found: tdd/reazed-306_live_occupancy_44f250a2/C1/refactor)
- [ ] In-batch non-overlapping due pair untested · `lib/reservations/auto-assign.ts` `planAutoAssignments` · two due reservations that do not overlap could share a label; C1 only pins already-held lunch vs due dinner · med · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C1/red)
- [ ] Half-open exclusive-end untested · `lib/reservations/auto-assign.ts` · 12:00 (90+15) vs 13:45 should reuse; C1 pins 12:00/19:00 and 18:00/18:30 only · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C1/refactor)
- [ ] Write-path lunch/dinner reuse untested · `tests/unit/reservations/assign-table.test.ts` · C2 only pins overlapping 19:00 vs 19:30 reject · med · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C2/refactor)
- [ ] Write path has no case for `seats > party_size` · `tests/unit/reservations/assign-table.test.ts` · FP-5 is `seats >= party_size`; C3 pins undersize reject and exact-fit only · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C3/red)
- [ ] Undersize copy is unpinned · `tests/unit/reservations/assign-table.test.ts` · `{ error: expect.any(String) }` allows empty or overlap-copy on the undersize path · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C3/refactor)
- [ ] Dropdown fit is unpinned for oversize tables · `tests/unit/reservations/selectable-tables.test.ts` · fixtures are 2/4/8 vs party 8, so `seats === partySize` would still pass · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C4/green)
- [ ] Available-or-current status filter is unpinned · `lib/reservations/selectable-tables.ts` · C4 only feeds `available` rows · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C4/green)
- [ ] Wiring pin is the function name, not argument shape · `tests/unit/reservations/selectable-tables.test.ts` · `/\bselectableTablesForAssignment\s*\(/` still passes if `partySize` were a wrong literal · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C4/refactor)
- [ ] C5 does not pin the stable messages · `tests/unit/reservations/get-by-date.test.ts` · `{ error: expect.any(String) }` still passes if `Unauthorized.` / `Could not load reservations.` change · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C5/refactor)
- [ ] Wiring pin is the helper name, not the error mapping · `tests/unit/reservations/list-empty-copy.test.ts` · `/\bstaffListEmptyCopy\s*\(/` still passes if the call omitted `error: listError` · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C6/refactor)
- [ ] Regex pin misses compound truthy expressions · `tests/unit/dev-toolchain/typecheck-toolchain.test.ts:39-41` · `(?!false\b|0\b)` lets `ignoreBuildErrors: false || true` (and `0 || true`) vacuous-pass; unlike G-L1 C2 the key need not be present, so a future Next default flip to `true` would also stay green until someone writes an explicit truthy literal · low · (found: tdd/reazed-278_ignorebuilderrors_7ba76e17.plan/C1/refactor)
- [ ] SQL merge collapse / OOS / assigned-label hard-claim have no integration pin · `tests/integration/reservations/table-fit.integ.test.ts` · C6 only asserts two overlapping parties of 8 plus a small unlabeled insert · med · (found: REAZED-305/C6/refactor)
- [ ] Oversize booking test never hits the trigger · `tests/integration/reservations/atomic-booking.integ.test.ts` · `totalCapacity + 1` is above `RESERVATION_ONLINE_MAX_PARTY` (8) so the online cap rejects first · med · (found: REAZED-305/C7/red)

# Test-debt findings (open)

- [ ] Integration harness does not load `.env.local` at all · `vitest.integration.config.ts` / `tests/integration/setup.ts` · no dotenv/`loadEnv` call and `envPrefix` isn't even set (it would only affect `import.meta.env`, not `process.env`, anyway), so strict runs collect 0 tests unless the shell already exported `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` · med · (seen: /triage 2026-08-25) (found: OH-SAVE/red)
- [ ] Snapshot restore is two client round-trips · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` · insert-then-delete still is not a single transaction; a failed delete can leave duplicate week rows · low · (seen: /triage 2026-08-25) (found: OH-SAVE/refactor)
- [ ] Inverted-window steal is unasserted · `tests/unit/reservations/operating-hours.test.ts` · the `closes_at` gate is implemented but no test pins that a `14:00–13:00` row cannot take `14:00` from a valid earlier segment · low · (seen: /triage 2026-08-25) (found: C1/refactor)
- [ ] Optional `durationMinutes` and invalid `start` untested · `slotUntilTime` · C2 pins default 90 and `23:00`→`00:30` only · low · (seen: /triage 2026-08-25) (found: C2/refactor)
- [ ] C4 pin does not assert trim-to-NULL · `tests/unit/scheduling/schema.test.ts` · regex only requires `guest_note` in the INSERT column list · low · (seen: /triage 2026-08-25) (found: C4/refactor)
- [ ] C5 pin is SQL-regex only · `tests/unit/branding/schema.test.ts` · does not execute INSERT of invalid values against a live DB · low · (seen: /triage 2026-08-25) (found: C5/refactor)
- [ ] `getAvailableSlots` has no unit coverage for blocked dates, closed days, past-today filtering, or capacity exhaustion · `app/actions/reservations.ts` · guest-facing branches untested · med · (seen: /triage 2026-08-25) (found: C7/red)
- [ ] C8 suite is source-regex only · `tests/unit/reservation-widget/segment-groups.test.ts` · windows-null wait, fallback group, unique keys are unpinned · med · (seen: /triage 2026-08-25) (found: C8/refactor)
- [ ] Grouping layer does not pin a shared `opens_at` boundary · `tests/unit/reservations/operating-hours.test.ts` (C6) · Midi/Soir do not overlap, so grouped output might skip `assignSegmentForTime` · low · (seen: /triage 2026-08-25) (found: C6/red)
- [ ] C11 does not execute the control or pin the getter · `tests/unit/floor/slot-interval.test.ts` · source-regex + one mocked upsert; round-trip `getSlotIntervalMinutes` is unasserted · med · (seen: /triage 2026-08-25) (found: C11/refactor)
- [ ] C1 is install-health, not a `tsc` regression · `tests/unit/dev-toolchain/typecheck-toolchain.test.ts:16-30` · asserts declare+lock+`existsSync(node_modules/swr)`; never runs `tsc`, so a present-but-unresolvable types entry still passes; also fails on a clean checkout until `pnpm install` · low · (seen: /triage 2026-08-25) (found: C1/green)
- [ ] No runtime coverage for Chef’s picks · `hooks/use-chefs-picks.ts` / `app/[locale]/page.tsx` · C2 only guards source types; `enabled` default, empty `items`, and `name_en` vs `name` locale fallback are untested · med · (seen: /triage 2026-08-25) (found: C2/red)
- [ ] No pin that `createServiceClient` stays unguarded · `lib/supabase/service.ts` · spec forbids putting isolation in the service client so staff Save against the linked project remains valid; C1 only covers the helper · med · (found: tdd/pr35_coderabbit_oh_save/C1/red)
- [ ] `assertIsolatedHoursMutationTarget(undefined)` is env-sensitive · `lib/scheduling/hours-mutation-target.ts` · treats `undefined` as missing URL, but the helper resolves `url ?? process.env.NEXT_PUBLIC_SUPABASE_URL`, so a runner with a local env URL (e.g. leaked from a prior shell command in the same session) would stop throwing — confirmed live during C3 refactor re-verification · low · (found: tdd/pr35_coderabbit_oh_save/C1/green, C3/refactor)
- [ ] Unbracketed `::1` is a dead allowlist entry · `lib/scheduling/hours-mutation-target.ts:10` · WHATWG `hostname` is `[::1]` for valid IPv6 URLs; entry is unreachable unless a non-WHATWG parser appears · low · (found: tdd/pr35_coderabbit_oh_save/C1/refactor)
- [ ] C3 is a source-regex, not an assignability check · `tests/unit/floor/layout.test.ts` · never type-checks `spreadOverlappingTables(useFloorPlan().tables)`; a stray `id: string` could pass without grid fields on default `AssignableTable` · low · (seen: /triage 2026-08-25) (found: C3/red)
- [ ] Ignore pin does not protect in-repo Supabase source · `tests/unit/dev-toolchain/lint-toolchain.test.ts:34-44` · a later `supabase/**` `globalIgnores` would still satisfy the two true asserts plus the `eslint.config.mjs` false assert · low · (found: C1/refactor)
- [ ] G-L1 flat-config pin accepts `eslint.config.js` · `tests/unit/dev-toolchain/lint-toolchain.test.ts:23-31` · spec G-L1 names root `eslint.config.mjs` only, but the existing test treats `.js` as sufficient, so a `.mjs`-less tree would still pass · low · (found: tdd/reazed-272_lint_max_warnings/C1/red)
- [ ] Lint-script pin is a substring, not an ESLint argv check · `tests/unit/dev-toolchain/lint-toolchain.test.ts:50` · `toContain("--max-warnings 0")` would still pass a script that never hands that flag to eslint (e.g. `echo "--max-warnings 0"`) · low · (found: tdd/reazed-272_lint_max_warnings/C1/refactor)
- [ ] No live assertion that **anon** cannot INSERT/UPDATE/DELETE `operating_windows` · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` · spec §16 forbids anon writes; C1 is authenticated-only and C2 is SQL-text · low · (found: tdd/reazed-290_hours_privilege/C1/red)
- [ ] `isHoursWritePermissionError` does not treat PostgREST `401`/`403` as `error.code` · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`isHoursWritePermissionError`) · INSERT after Green could fail for the wrong reason if the client surfaces HTTP 401/403 without 42501 / PGRST301 / a matching message · low · (found: tdd/reazed-290_hours_privilege/C1/red)
- [ ] Nested `afterAll` REVOKEs SELECT with DML · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`HOURS_DATA_API_REVOKE`) · C2 `GRANT SELECT` is now in baseline; this REVOKE would strip it for the rest of the Vitest process until the next reset · med · (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] Integ suite docker-GRANTs then REVOKEs `service_role` DML · `tests/integration/scheduling/replace-operating-windows.integ.test.ts:16-19,107,168` · `afterAll` `REVOKE SELECT, INSERT, UPDATE, DELETE … FROM service_role` now strips the shipped GRANT ALL and re-opens 42501 on local until the next `db reset` · med · (found: tdd/reazed-297_service-role_grant_5bde11c5/plan, C1/refactor)
- [ ] Guest SELECT pin runs under the suite’s temporary docker GRANT, not baseline ACL · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (anon select at end of C1 `it`) · assertion does not prove public-read without that GRANT even though C2 now GRANTs SELECT in baseline · low · (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] Hardcoded Docker db container name · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`supabase_db_restaurant-system`) · a renamed local project cannot apply the ACL overlay, so the live RLS probe never runs · low · (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] C2 `toContain` still passes if GRANT SELECT lists extra roles · `tests/unit/scheduling/schema.test.ts` (grant/revoke strings) · `TO anon, authenticated, service_role` would still match; folding REAZED-297 into the SELECT grant would not fail C2 · low · (found: tdd/reazed-290_hours_privilege/C2/green)
- [ ] Forward file could re-CREATE authenticated FOR ALL after DROP and C2 would still pass · `tests/unit/scheduling/schema.test.ts:78-80` · `toContain` only requires the DROP POLICY substring; the remote-only apply path would restore writes · med · (found: tdd/reazed-290_hours_privilege/C2/refactor)
- [ ] C2 does not pin `NOTIFY pgrst, 'reload schema'` · `tests/unit/scheduling/schema.test.ts:62-81` / `20260825140000_operating_windows_privilege.sql:13` · removing the notify would still pass the unit contract while PostgREST on the linked remote could serve a stale grant cache · low · (found: tdd/reazed-290_hours_privilege/C2/refactor)
- [ ] C2 REVOKE is never live-proven · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` · C1 re-GRANTs INSERT/UPDATE/DELETE before the probe, so it asserts RLS deny, not `has_table_privilege` false; that check remains manual-UAT · med · (found: tdd/reazed-290_hours_privilege/C2/refactor)

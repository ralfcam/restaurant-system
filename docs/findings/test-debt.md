# Test-debt findings (open)

- [ ] No pin that `createServiceClient` stays unguarded · `lib/supabase/service.ts` · spec forbids putting isolation in the service client so staff Save against the linked project remains valid; C1 only covers the helper · med · (seen: /triage 2026-08-26) (found: tdd/pr35_coderabbit_oh_save/C1/red)
- [ ] `assertIsolatedHoursMutationTarget(undefined)` is env-sensitive · `lib/scheduling/hours-mutation-target.ts` · treats `undefined` as missing URL, but the helper resolves `url ?? process.env.NEXT_PUBLIC_SUPABASE_URL`, so a runner with a local env URL (e.g. leaked from a prior shell command in the same session) would stop throwing — confirmed live during C3 refactor re-verification · low · (seen: /triage 2026-08-26) (found: tdd/pr35_coderabbit_oh_save/C1/green, C3/refactor)
- [ ] Unbracketed `::1` is a dead allowlist entry · `lib/scheduling/hours-mutation-target.ts:10` · WHATWG `hostname` is `[::1]` for valid IPv6 URLs; entry is unreachable unless a non-WHATWG parser appears · low · (seen: /triage 2026-08-26) (found: tdd/pr35_coderabbit_oh_save/C1/refactor)
- [ ] Ignore pin does not protect in-repo Supabase source · `tests/unit/dev-toolchain/lint-toolchain.test.ts:34-44` · a later `supabase/**` `globalIgnores` would still satisfy the two true asserts plus the `eslint.config.mjs` false assert · low · (seen: /triage 2026-08-26) (found: C1/refactor)
- [ ] G-L1 flat-config pin accepts `eslint.config.js` · `tests/unit/dev-toolchain/lint-toolchain.test.ts:23-31` · spec G-L1 names root `eslint.config.mjs` only, but the existing test treats `.js` as sufficient, so a `.mjs`-less tree would still pass · low · (seen: /triage 2026-08-26) (found: tdd/reazed-272_lint_max_warnings/C1/red)
- [ ] Lint-script pin is a substring, not an ESLint argv check · `tests/unit/dev-toolchain/lint-toolchain.test.ts:50` · `toContain("--max-warnings 0")` would still pass a script that never hands that flag to eslint (e.g. `echo "--max-warnings 0"`) · low · (seen: /triage 2026-08-26) (found: tdd/reazed-272_lint_max_warnings/C1/refactor)
- [ ] No live assertion that **anon** cannot INSERT/UPDATE/DELETE `operating_windows` · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` · spec §16 forbids anon writes; C1 is authenticated-only and C2 is SQL-text · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C1/red)
- [ ] `isHoursWritePermissionError` does not treat PostgREST `401`/`403` as `error.code` · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`isHoursWritePermissionError`) · INSERT after Green could fail for the wrong reason if the client surfaces HTTP 401/403 without 42501 / PGRST301 / a matching message · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C1/red)
- [ ] Nested `afterAll` REVOKEs SELECT with DML · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`HOURS_DATA_API_REVOKE`) · C2 `GRANT SELECT` is now in baseline; this REVOKE would strip it for the rest of the Vitest process until the next reset · med · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] Integ suite docker-GRANTs then REVOKEs `service_role` DML · `tests/integration/scheduling/replace-operating-windows.integ.test.ts:16-19,107,168` · `afterAll` `REVOKE SELECT, INSERT, UPDATE, DELETE … FROM service_role` now strips the shipped GRANT ALL and re-opens 42501 on local until the next `db reset` · med · (seen: /triage 2026-08-26) (found: tdd/reazed-297_service-role_grant_5bde11c5/plan, C1/refactor)
- [ ] Guest SELECT pin runs under the suite’s temporary docker GRANT, not baseline ACL · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (anon select at end of C1 `it`) · assertion does not prove public-read without that GRANT even though C2 now GRANTs SELECT in baseline · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] Hardcoded Docker db container name · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` (`supabase_db_restaurant-system`) · a renamed local project cannot apply the ACL overlay, so the live RLS probe never runs · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C1/refactor)
- [ ] C2 `toContain` still passes if GRANT SELECT lists extra roles · `tests/unit/scheduling/schema.test.ts` (grant/revoke strings) · `TO anon, authenticated, service_role` would still match; folding REAZED-297 into the SELECT grant would not fail C2 · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C2/green)
- [ ] Forward file could re-CREATE authenticated FOR ALL after DROP and C2 would still pass · `tests/unit/scheduling/schema.test.ts:78-80` · `toContain` only requires the DROP POLICY substring; the remote-only apply path would restore writes · med · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C2/refactor)
- [ ] C2 does not pin `NOTIFY pgrst, 'reload schema'` · `tests/unit/scheduling/schema.test.ts:62-81` / `20260825140000_operating_windows_privilege.sql:13` · removing the notify would still pass the unit contract while PostgREST on the linked remote could serve a stale grant cache · low · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C2/refactor)
- [ ] C2 REVOKE is never live-proven · `tests/integration/scheduling/replace-operating-windows.integ.test.ts` · C1 re-GRANTs INSERT/UPDATE/DELETE before the probe, so it asserts RLS deny, not `has_table_privilege` false; that check remains manual-UAT · med · (seen: /triage 2026-08-26) (found: tdd/reazed-290_hours_privilege/C2/refactor)

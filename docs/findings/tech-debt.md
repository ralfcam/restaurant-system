# Tech-debt findings (open)

- [ ] Privilege forward filename still hours-only · `supabase/migrations/20260825140000_operating_windows_privilege.sql` · file now also GRANTs `blocked_dates` / `reservations` / `menu_items`; renaming a dated migration is unsafe · low · (seen: /triage 2026-08-27) (found: tdd/reazed-297_sibling_grants_097b94f3/C1/green)
- [ ] Optional `leadMinutes` can bypass the pinned default · `lib/reservations/auto-assign.ts:96` and `:147` · FP-2 is restaurant-default 90 before a table is chosen; the optional override lets a future caller pass 15 without changing `TABLE_ASSIGNMENT_LEAD_MINUTES` (C1’s equality pin would still pass) · low · (seen: /triage 2026-08-27) (found: tdd/reazed-307_fp2_lead_8387cb6a/C1/green)
- [ ] Tonight’s book helper is a hardcoded minute count, not `DEFAULT_EXPECTED_MINUTES` · `components/staff/floor-plan.tsx:917-918` · staff copy now says default 90 but is not the C1 due-check constant, so the numeral can drift independently · low · (seen: /triage 2026-08-27) (found: tdd/reazed-307_fp2_lead_8387cb6a/C2/red)
- [ ] unused getReservations still uses cookie createClient() · app/actions/reservations.ts ~494 · unused by admin pages (they call getReservationsByDate + service_role); after REVOKE SELECT FROM authenticated this path would 42501 · low · (found: REAZED-308/plan)
- [ ] `slotUntilTime` / `nextBookableTime` still add unclamped minutes · `lib/reservations/operating-hours.ts` · out-of-range occupancy or buffer still changes badge/next-bookable until a caller clamps; these helpers themselves do not · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C3/green)
- [ ] occupancy forward omits `NOTIFY pgrst, 'reload schema'` · `supabase/migrations/20260827180000_occupancy_duration_buffer.sql` · privilege dated forwards notify; column forwards (`hero_image_url`, chefs-picks) do not — a long-lived remote PostgREST may not expose the new columns until restart · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C4/refactor)
- [ ] `tables.expected_minutes` CHECK is range-only · `supabase/migrations/00000000000000_baseline.sql` (`CHECK (expected_minutes BETWEEN 30 AND 240)`) · `clampExpectedMinutes` steps by 15, so 31 is valid in the DB and invalid in the clamp; C4 encodes step on occupancy, this column still will not · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C4/red)
- [ ] Floor upcoming list still inlines occupying statuses · `components/staff/floor-plan.tsx:206` · `row.status === "confirmed" || row.status === "seated"` is a third copy of BW-10; a later occupancy-set change can desync the floor from slots/auto-assign · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C7/refactor)
- [ ] Occupying set lives in auto-assign · `lib/reservations/auto-assign.ts:20` · availability now imports a floor-assignment module for a booking-rules constant; C8 SQL is a fourth copy (`confirmed`/`seated` literals) · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C7/refactor)
- [ ] `clampSafetyBufferMinutes` fail-to-default footgun · `lib/reservations/operating-hours.ts` `clampSafetyBufferMinutes` · values outside 0–60 (including `60 + 5`) return 15, not 60, so the floor stepper had to pre-clamp; any other `current + step` caller can silently reset the buffer · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/green)
- [ ] `React.cache()` for the shared settings loader lives in a `"use server"` module · `app/actions/branding.ts:320` · Next.js may not provide a React cache store if those getters run as action POSTs, so the three named page awaits could still triple-query · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C5/refactor)
- [ ] Trigger body remains triplicated · baseline + `20260818162000_operating_hour_segments.sql` + `20260827180000_occupancy_duration_buffer.sql` · Postgres migrations have no include; a later hours or occupancy edit that touches only one last-writer will silently fork confirm-path enforcement · med · (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/refactor)
- [ ] No index on occupancy cover keys · `reservations` has no `(date)` / `(date, status)` index · every INSERT seq-scans the table for overlapping `confirmed`/`seated` rows · low · (found: tdd/reazed-309_occupancy_buffer_1862f023/C8/refactor)
- [ ] Unvalidated tables.status cast then unguarded increment · `app/actions/operations.ts:104` (`mapTable`) + `lib/floor/table-use.ts:158` · unknown DB status becomes NaN on Floor status · med · (found: tdd/reazed-306_live_occupancy_44f250a2/C1/refactor)
- [ ] No DB exclusion / lock on overlapping `table_label` · concurrent staff assigns can race the TS check · med · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/plan/seed)
- [ ] Duplicate accessible name on the assign control · `components/staff/reservations-manager.tsx:397` + `:407` · `sr-only` span and `aria-label` repeat the same string · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C4/refactor)
- [ ] Date-fetch has no `.catch` · `components/staff/reservations-manager.tsx` · thrown network error can leave `loadingDate` stuck · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/plan/seed)
- [ ] Zero-length occupying window treated as rest-of-day · `lib/reservations/auto-assign.ts` `occupyingWindowMinutes` maps `endMin <= startMin` (including occupancy+buffer = 0) to `24 * 60` · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C1/green)
- [ ] Assigned row with unparseable time fails open · `lib/reservations/auto-assign.ts` · null window skips the claim, so a held label with a bad `time` does not occupy · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C1/refactor)
- [ ] Unparseable reservation time skips the overlap reject · `app/actions/reservations.ts` `assignReservationTable` `if (window)` · null window fail-opens the write · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C2/green)
- [ ] Occupant unparseable `time` fail-opens · `app/actions/reservations.ts` · a bad occupant time is treated as non-conflicting · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C2/refactor)
- [ ] Settings fetch error ignored · `app/actions/reservations.ts` `assignReservationTable` · settings `error` is dropped and falls through to 90+15 · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C2/refactor)
- [ ] Seat check fails open if `seats`/`party_size` is non-numeric · `app/actions/reservations.ts` · `undefined < n` is false, so a missing numeric `seats` still assigns · low · (found: tdd/reazed-300-staff-reservations_3fd7b670.plan/C3/green)

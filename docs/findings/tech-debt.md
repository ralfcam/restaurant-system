# Tech-debt findings (open)

- [ ] Branding settings writes throw `Unauthorized` instead of returning `{ error: "Unauthorized." }` · `app/actions/branding.ts` `upsertRestaurantSetting` · Staff persist APIs disagree (review-email follows reservations, not branding); leftover for a later branding-cms pass · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C1/red)
- [ ] `restaurant_settings` has no `review_email_*` columns · `supabase/migrations/00000000000000_baseline.sql` (`restaurant_settings`) · C1 upserts `review_email_enabled` / `_copy` / `_maps_url` / `_delay_hours` against a mocked client; a real write will fail until those columns exist. No later criterion in this plan owns this DDL · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C1/green)
- [ ] `review_email_sends` table is not in schema · `supabase/migrations/00000000000000_baseline.sql` · C3/C4 read/insert this table against mocks; a real complete/processDue will fail until the table exists. No later criterion owns this DDL · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C4/green)
- [ ] `reservations.completed_at` column missing from baseline · `supabase/migrations/00000000000000_baseline.sql` (`reservations`) · `transitionReservationStatus` now writes `completed_at`; a real complete will fail until the nullable timestamptz exists. C10 owns guest-email SELECT, not this column · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C5/green)
- [ ] Enqueue insert errors are ignored · `app/actions/reservations.ts:294` · Insert errors are now logged, but the action still returns `{}`, so staff can complete with no send row and no user-facing error · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C4/green)
- [ ] Save does not `revalidatePath` · `app/actions/marketing.ts` · Sibling settings writes revalidate; C11 shipped the page without adding it · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C1/refactor)
- [ ] TDD red write guard rejects Windows absolute `tests/**` paths · `.cursor/hooks` tdd-guard · a legitimate `tests/unit/...` write can be reported outside `tests/**` because the path used backslashes · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C2/red)
- [ ] `isPermissionError` duplicated instead of shared · `tests/integration/reservations/review-email-pii.integ.test.ts` vs `public-privileges.integ.test.ts` · two copies of the 42501 / PGRST301 matcher will drift independently · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C10/red)
- [ ] Nested embed typed as a single object · `lib/marketing/review-email.ts` `.select("…, reservations(email, status)")` · without a typed many-to-one FK, PostgREST/supabase-js can return an array; `reservation?.email` would then be undefined and every row would skip · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C3/refactor)
- [ ] Persist does not clamp delay_hours · `app/actions/marketing.ts:30` · Staff can upsert 73/−1/NaN; send then treats them as 24, so the saved value and the clock disagree · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C5/refactor)
- [ ] StaffShell nav is three independent href allowlists · `components/staff/staff-shell.tsx:70-88` (`NAV_GROUPS`) and `:134-136` (`MobileBottomNav`) · a `NAV` entry does not render unless its group filter lists the href; C11 guards Setup for marketing only, so later items and the mobile quick bar can still silently drop · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C11/red)
- [ ] Marketing form is write-only · `app/admin/marketing/review-email-settings-form.tsx:15-16` · Defaults stay off / empty / 24h; after `review_email_*` columns exist, saved values still will not round-trip · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C11/green)
- [ ] Enable Label click does not toggle the Switch · `app/admin/marketing/review-email-settings-form.tsx:51-59` · Accessible name is `aria-labelledby`; the Label has no `htmlFor`/click handler, so only the thumb toggles · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C11/refactor)
- [ ] One send failure aborts the rest of the due batch · `lib/marketing/review-email.ts:110` (`await mailer.send()` with no per-row try/catch) · a provider throw skips later due reservations in the same run; C6 only covers retry of the same row · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C6/red)
- [ ] Non-throwing mailer failure is treated as success · `lib/marketing/review-email.ts:111` · Any settled `send()` stamps `sent_at`; a future `{ ok: false }` port would never retry · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C6/refactor)
- [ ] `to` is not trimmed · `lib/marketing/review-email.ts` send payload · empty-email gate uses `email.trim() === ""` but send uses the raw string, so padded addresses can go to the mailer · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C7/refactor)
- [ ] `ReservationRow` omits `email` · `app/actions/reservations.ts:52` · column now exists; staff typed rows and selects will not surface it · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C10/green)
- [ ] Folded baseline will not apply `email` to already-migrated remotes · linked ref `tilcqrudqxznnpepxjqq` · a forward dated file was skipped (fold policy); remote PostgREST has no `email` until reset or a later forward migration · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C10/green)
- [ ] Undo leaves `completed_at` set · `app/actions/reservations.ts:348` · After completed→seated undo the timestamp stays; send still skips on status, but a later complete is the only overwrite · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C5/refactor)
- [ ] `.cursor/rules/vercel-project.mdc` env list still omits `CRON_SECRET` · `.cursor/rules/vercel-project.mdc` Env targets · `docs/runbooks/deploy.md` now lists it; the rule still names only the three Supabase keys · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C9/green)
- [ ] Cron route mailer always throws · `app/api/cron/review-email/route.ts:13` · authorized GET still uses a throwing stub; due emails never leave even with `CRON_SECRET` set (and a due row makes the handler 500) · med · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C9/green)
- [ ] Cron GET has no observability · `app/api/cron/review-email/route.ts:4` · 401 vs 200 vs thrown stub are indistinguishable in runtime logs · low · (seen: /triage 2026-08-30) (found: tdd/post-visit_review_tdd_ac1962e1/C9/refactor)
- [ ] Seed auth.users.email is empty while identity email is admin@test.local · supabase/seed.sql:32 · confusing; login may still work via identities; tooling that reads the users table can disagree with identities · low · (seen: /triage 2026-08-30) (found: REAZED-310/planning)
- [ ] Redirect responses drop refreshed session cookies · lib/supabase/proxy.ts:39-42 · `setAll` writes rotated cookies onto `supabaseResponse`, but the staff-path redirect still returns a fresh `NextResponse.redirect` without copying them; a refresh-token rotation on `/admin|/pos|/kds` can be lost · med · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C2/green)
- [ ] Staff prefixes duplicated across locale skip and the proxy gate · lib/supabase/proxy.ts:5 vs i18n/middleware-scope.ts:3-8 · `/admin|/pos|/kds` listed twice with different extra members (`/api`, `/auth`); a new staff surface could be gated but still localized, or the reverse · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C2/refactor)
- [ ] Local Auth does not reload `config.toml` until the stack is restarted · supabase/config.toml · Green can flip both flags and the unit test will pass while an already-running local GoTrue still accepts public signup until `npx supabase stop; npx supabase start` · med · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C4/red)
- [ ] Hosted config-push maps `[auth.email] enable_signup` to the whole email provider · supabase/config.toml:221 (upstream `ExternalEmailEnabled`) · Local CLI keeps the provider on for `supabase start`, but pushing this toml (or mirroring both keys on hosted) can yield `email_provider_disabled` and block seed staff password login. SA-6 must use the dashboard signup toggle, not a blind config copy · med · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C4/refactor)
- [ ] Seed claim is insert-only · supabase/seed.sql:46 · `ON CONFLICT (id) DO NOTHING` will not backfill `role` on an existing local `auth.users` row; only a full `db reset` applies the claim · low · (seen: /triage 2026-08-30) (found: tdd/reazed-310_staff_auth_a6b9be91.plan/C5/refactor)
- [ ] Crash or failed release after claim skips retry · `lib/marketing/review-email.ts:131-157` · process death between CAS and send, or a failed/ignored compensating `sent_at: null` write (supabase-js does not throw on `{ error }`), leaves the row claimed so PV-6 retries never run; no reclaim-timeout this run · med · (found: mailing_automations_triple_2347e1d5.plan/planning)
- [ ] PV-6 implementation trace still documents stamp-after-send · `docs/specs/post-visit-review-email.md` (PV-6 table row) · Criterion prose now requires exclusive claim before the mailer, but the trace still says stamp after `mailer.send()`; docs-updater cannot write `docs/specs/**` · med · (found: mailing_automations_triple_2347e1d5.plan/C2/red)
- [ ] PV-9 implementation trace omits `server-only` · `docs/specs/post-visit-review-email.md` (PV-9 table row) · code has `import "server-only"` and is not `"use server"`; the trace still only lists cron Bearer / anon SELECT · med · (found: mailing_automations_triple_2347e1d5.plan/4-docs)
- [ ] booking-rules Implementation trace missing AC-4 / BW-13 / BW-14 · `docs/specs/booking-rules.md` · BW-7 still says “same (Réserver gate)”; shipped email-required / confirmation-send is not in the table · med · (found: mailing_automations_triple_2347e1d5.plan/4-docs)
- [ ] Guest email has no length cap · `lib/reservations/validation.ts` · name/notes are capped (100/500); unbounded email can be persisted once C4 writes the column · low · (found: mailing_automations_triple_2347e1d5.plan/C3/green)
- [ ] Confirmation send failures are swallowed with no log · `app/actions/reservations.ts` (success-path `catch`) · insert errors use `console.error`, but a throwing stub/provider leaves no operational signal that confirmations never went out · med · (found: mailing_automations_triple_2347e1d5.plan/C6/green)
- [ ] Duplicated `escapeHtml` · `lib/marketing/booking-confirmation.ts` vs `lib/marketing/review-email.ts:35` · two copies can drift on entity order · low · (found: mailing_automations_triple_2347e1d5.plan/C7/green)
- [ ] email has no `typeof === "string"` guard · `lib/reservations/validation.ts:90` · date/time reject non-strings; a non-string `email` throws on `.trim()` instead of a reservation error · low · (found: mailing_automations_triple_2347e1d5.plan/C3/refactor)
- [ ] Public action can throw on omitted phone · `app/actions/reservations.ts:135` · validation accepts missing/blank phone (`payload.phone?.trim() ?? ""`) but insert does `payload.phone.trim()` with no nullish fallback; omitting `phone` on the unauthenticated action can throw before INSERT · med · (found: mailing_automations_triple_2347e1d5.plan/C5/refactor)

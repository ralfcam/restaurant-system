# Tech-debt findings (open)

- [ ] Completed-status enqueue errors are swallowed · `app/actions/reservations.ts:316-320` · `review_email_sends` insert failure after a successful status update is only `console.error`; the guest is marked completed with no claim row · med · (found: tdd/res-45_review_email_schema_cron_7f3a91c2/C2/green)
- [ ] cron full-scans unsent rows · `lib/marketing/review-email.ts:103-108` · `.is("sent_at", null)` has no partial index; every hourly tick seq-scans the queue · low · (found: tdd/res-45_review_email_schema_cron_7f3a91c2/C2/refactor)
- [ ] `ReservationRow` omits `completed_at` · `app/actions/reservations.ts:56-68` · `select("*")` casts drop the clock at the type boundary; complete write uses `Record<string, unknown>` · low · (found: tdd/res-45_review_email_schema_cron_7f3a91c2/C3/refactor)
- [ ] Cron GET still runs unused Auth `getUser()` via catch-all proxy · `proxy.ts` / `lib/supabase/proxy.ts` · the hourly schedule makes this path production-hot; an Auth hang 500s the job before the Bearer check · low · (found: tdd/res-45_review_email_schema_cron_7f3a91c2/C5/refactor)
- [ ] POS page load can rewrite floor layout · `app/actions/operations.ts:264-275` via `app/pos/page.tsx:11` · `getTables()` still runs `spreadOverlappingTables` and persists new `x`/`y`; every `/pos` request can mutate dining-room coordinates, not only `/admin/floor` · med · (seen: /triage 2026-09-02) (found: tdd/reazed-316_pos_table_picker_28440958/FP-13/refactor)
- [ ] Full `PersistedTable` rows serialized into the client picker · `components/staff/pos-terminal.tsx:30-34` · picker only uses `id`/`label`; seats, status, mergeId, and coordinates still cross the RSC boundary · low · (seen: /triage 2026-09-02) (found: tdd/reazed-316_pos_table_picker_28440958/FP-13/refactor)
- [ ] Table/Server labels are not associated with the selects · `components/staff/pos-terminal.tsx:143` / `:158` · no `htmlFor` / `id`; pre-existing a11y gap, not FP-13 · low · (seen: /triage 2026-09-02) (found: tdd/reazed-316_pos_table_picker_28440958/FP-13/refactor)
- [ ] Staff chrome prefixes are listed twice · `i18n/middleware-scope.ts:3` vs `lib/supabase/proxy.ts:5` (`STAFF_PATHS`) · locale skip and staff-auth gate can drift independently if a new staff route is added to only one list · low · (seen: /triage 2026-09-02) (found: REAZED-280/C1/green)
- [ ] Predicate argument types diverge · lib/supabase/is-staff-user.ts:8 vs :13 · `isStaffUser` takes `User`; `isSuperAdminUser` takes `object` plus a cast so C1’s partial fixtures type-check. Aligning types later needs fixture `as User` (test change). · low · (seen: /triage 2026-09-02) (found: tdd/reazed-326_super_admin_permission_model/C1/green)
- [ ] LanguageSwitcher variant still keys off scroll alone · `components/site/site-header.tsx:89` · Unscrolled no-hero and scrolled both use dark text, but one stays `ghost` and the other flips to `outline`; SC-4a only required text color · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C334-2/green)
- [ ] Branding editors mutate without a handler `isSuperAdmin` guard · `components/staff/restaurant-logo-editor.tsx` / `restaurant-hero-image-editor.tsx` · Disabled buttons only; scheduling has an early-return but siblings can still invoke the action from the client · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C332-2/refactor)
- [ ] Prettier has no SQL parser (`.prettierrc.json` has no overrides/plugin) · required `--check` on `00000000000000_baseline.sql` / `seed.sql` cannot go green · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C329-1/refactor)
- [ ] Kitchen send still posts the display `name`, not `id` · `components/staff/pos-terminal.tsx:78` · unique `name` makes this safe today, but the picker already has `id` and the action is name-coupled · low · (found: tdd/ux_staffchrome_pos_batch_9c4a1b/C329-3/refactor)
- [ ] `lib/order-store.ts` and `lib/menu-store.ts` are fully unimported dead code · `lib/order-store.ts`, `lib/menu-store.ts` · BroadcastChannel/localStorage client stores were superseded by live Supabase-backed order/menu flows but never removed; a reader might assume they are the live sync mechanism, and they are easy to wire by mistake after the orders schema lands · low · (found: REAZED-312/planning/triage)
- [ ] `orders.table_id` has no `ON DELETE` action · `supabase/migrations/00000000000000_baseline.sql:614` · Default `NO ACTION` means deleting a floor table that still has kitchen tickets will fail; POS already stores `table_id` as nullable and keeps `table_label` · med · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/312-C1/green)
- [ ] `order_number` is `BIGSERIAL` NOT NULL without UNIQUE · `supabase/migrations/00000000000000_baseline.sql:613` · Sequence defaults are not a uniqueness guarantee if a client inserts an explicit number · med · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/312-C1/green)
- [ ] Unindexed FKs on `order_items.order_id` and `orders.table_id` · `supabase/migrations/00000000000000_baseline.sql:614,628` · Postgres does not index FKs; KDS nested `order_items` and ON DELETE CASCADE will seq-scan as volume grows · med · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/312-C1/refactor)
- [ ] Money columns are unbounded `NUMERIC` · `supabase/migrations/00000000000000_baseline.sql:617-619,631` · skill prefers `numeric(10,2)` for exact money · low · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/312-C1/refactor)
- [ ] `menu_item_id TEXT` is not an FK to `menu_items` · `supabase/migrations/00000000000000_baseline.sql:629` · orphaned line names/prices if catalog ids change · low · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/312-C1/refactor)
- [ ] Full `MenuItemRow` serialized into the client POS tree · `components/staff/pos-terminal.tsx:28` · picker/pricing only need `id`, `name`, `price`, `price_value`, `menu_id`; descriptions/slugs/etc. cross the RSC boundary unused · low · (found: tdd/reazed-311-312_pos-catalog-persistence_7c3f9a1d/311-C1/refactor)
- [ ] Header-visibility helper ignores locale-prefixed admin · `lib/site-chrome.ts:18` · `!pathname.startsWith("/admin")` would still show the guest header on `/en/admin` if staff login were ever wired through next-intl `Link` · low · (found: tdd/res-61_guest_i18n_followups/C1/refactor)
- [ ] Root `headers()` makes the whole tree dynamic · `app/layout.tsx:82` · `await headers()` in the root layout opts every route, including staff chrome, out of static rendering · med · (found: tdd/res-61_guest_i18n_followups/C5/green)
- [ ] Staff lang depends on a synthetic `/admin` pathname · `app/layout.tsx:73-74` · `proxy.ts` never sets `x-url`; if a public request also lacks `X-NEXT-INTL-LOCALE`, the page is announced as English · med · (found: tdd/res-61_guest_i18n_followups/C5/green)
- [ ] Primary pathname headers are unset in-repo · `app/layout.tsx:53-56` · no writer for `x-url` / `x-pathname` / `next-url`; every live request falls through to `x-next-intl-locale` or the `/admin` default · med · (found: tdd/res-61_guest_i18n_followups/C5/refactor)
- [ ] `/en` is hardcoded instead of `routing.locales` · `lib/i18n/document-lang.ts:8` · `localized-pathname.ts` already walks `routing`; a later locale would keep `html lang="fr"` on prefixed public paths · low · (found: tdd/res-61_guest_i18n_followups/C5/refactor)
- [ ] Four identical `setMobileMenuOpen(false)` lambdas in the sheet · `components/site/site-header.tsx:132` · C6 added a fourth copy · low · (found: tdd/res-61_guest_i18n_followups/C6/refactor)
- [ ] Unparameterized `MouseEventHandler` (defaults to `Element`) · `components/site/language-switcher.tsx:18` · slightly wide vs `ComponentProps<typeof Button>["onClick"]` · low · (found: tdd/res-61_guest_i18n_followups/C6/refactor)
- [ ] Guest e2e cannot boot without Supabase keys · `lib/supabase/proxy.ts:10` · `createServerClient` runs on every request, so public i18n pages (and the navbar e2e) hard-depend on env even when no DB work is needed · med · (found: tdd/res-61_guest_i18n_followups/C3/red)
- [ ] Prefixed default-locale path would not count as active · `lib/i18n/localized-pathname.ts:7-8` · `stripLocalePrefix` skips `fr`, so `isActiveNavPath("/fr/menu", "/menu")` is false; `as-needed` should not serve `/fr/menu` · low · (found: tdd/res-61_guest_i18n_followups/C4/refactor)
- [ ] SWR still uses default mount revalidation with `fallbackData` · `hooks/use-chefs-picks.ts:16` · HP-2 seeds first paint, but a default revalidate can refetch immediately after hydrate · low · (found: tdd/res-59_chefs_picks_ssr_avatar/C1/refactor)
- [ ] Root `usePathname` without Suspense · `app/layout.tsx:92` · Next.js flags Suspense around `usePathname` on dynamic routes; omitted because `await headers()` already makes the root dynamic and the child returns `null` · low · (found: tdd/html_lang_pnpm_pin_70d248bc/C1/refactor)
- [ ] Cloud Agent install does not name `pnpm@12.3.4` · `.cursor/environment.json` (`corepack prepare --activate`) · Relies on Corepack reading `package.json`; the G-O1 unit file only checks those substrings, not a versioned `corepack prepare pnpm@12.3.4` · low · (found: tdd/html_lang_pnpm_pin_70d248bc/C2/refactor)

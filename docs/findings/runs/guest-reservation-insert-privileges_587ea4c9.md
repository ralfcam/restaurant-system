# Findings run: guest-reservation-insert-privileges_587ea4c9

Merged to `docs/findings/{security,test-debt}.md` at close-out. `Public INSERT RLS is unconstrained` archived as resolved. REGISTER skipped for net-new Linear (operator confirmation required). `conf_code` UNIQUE left on `security.md` (high, above floor). C1 `id`/`created_at` assert left on `test-debt.md` (med, below non-security floor). Run scratch truncated.

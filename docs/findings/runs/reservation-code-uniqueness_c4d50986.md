# Findings run: reservation-code-uniqueness_c4d50986

Merged to `docs/findings/{security,test-debt}.md` at close-out.
`conf_code` UNIQUE archived as resolved in-run.
This run's `createReservation` 23505 retry coverage merged to `test-debt.md` (med, below high-only floor — left on ledger).
Hostile-test lookup-by-conf_code left untouched on `test-debt.md`.
Run scratch truncated.

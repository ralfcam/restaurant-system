# Findings run: html_lang_pnpm_pin_70d248bc

Merged to `docs/findings/{tech-debt,test-debt}.md` at close-out. REGISTER skipped — all four items below filing floor (`low`; no `high` / no security `med`). Run scratch truncated.

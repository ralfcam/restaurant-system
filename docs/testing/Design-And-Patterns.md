# Design & patterns

**Status:** Reference  
**Last updated:** 2026-09-01

Catalog of reusable test recipes promoted from shipped `/sdd-to-tdd` runs.

## Unit recipes (promoted from REAZED-…)

| Scenario                                                   | Mocks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Reference test                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Site chrome config — no bundled mark                       | `as const satisfies Pick<ImageProps, …>` without `src`; unit-test dimensions, absence of `src`, and template identity (`RESTAURANT.name`, `menu-catalog` rename guard)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tests/unit/site-chrome.test.ts`                                                                                                                                                                                                                                                                  |
| Filesystem composition guard (no duplicate chrome / route) | `existsSync` (flat route absent) + `readFileSync` + regex on `app/[locale]/page.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tests/unit/site-header.test.ts` → "no stale flat app/page.tsx duplicate; localized homepage delegates to SiteHeader without inline fixed header"                                                                                                                                                 |
| Source-structure regression (shared chrome ↔ e2e hooks)    | `readFileSync` + regex on component source; assert import and JSX presence for chrome wired to `data-testid` hooks exercised in e2e                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tests/unit/i18n/site-header-switcher.test.ts` → "site header renders LanguageSwitcher in shared navbar"                                                                                                                                                                                          |
| Route visibility helper synced with component              | Export `shouldRenderSiteHeader(pathname)` from `lib/`; component calls same helper                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/site-header.test.ts` (same test as composition guard — asserts `shouldRenderSiteHeader("/")`)                                                                                                                                                                                         |
| Server Action body limit synced with upload cap            | `readFileSync` + regex on `next.config.mjs` for `bodySizeLimit` matching `LOGO_UPLOAD_BODY_SIZE_LIMIT` in `lib/branding.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/branding/schema.test.ts` → "raises the Server Action body limit so a 2MB logo fits as base64"                                                                                                                                                                                         |
| Storage upload retries after missing bucket                | Mock `storage.from().upload` to fail once with "Bucket not found", `createBucket` succeeds, second upload succeeds; assert `Uint8Array` payload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `tests/unit/branding/actions.test.ts` → "creates the branding bucket when it is missing, then uploads"                                                                                                                                                                                            |
| MIME alias / empty browser `type` validation               | Unit-test `resolveLogoContentType` and `validateLogoUpload` with `image/jpg`, `image/x-png`, and extension-only file names                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/unit/branding/validation.test.ts`                                                                                                                                                                                                                                                          |
| No bundled static logo in `public/`                        | `existsSync` on `public/images/logo.png` and `.jpg`; assert absent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/branding/schema.test.ts` → "staff branding page exists and leftover test fixtures are gone"                                                                                                                                                                                           |
| Guest chrome uses `BrandMark`, not `SITE_LOGO.src`         | `readFileSync` + regex on `site-header.tsx` and `auth/login/page.tsx`; assert `<BrandMark src={logoUrl}>` and no `SITE_LOGO.src`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `tests/unit/branding/schema.test.ts` → "guest header and login render BrandMark only when a custom url is set"                                                                                                                                                                                    |
| Clock-face wrap before `minutesToTime`                     | Until-badge (`slotUntilTime`) and next-bookable (`nextBookableTime`) are separate exports sharing private `addMinutesWrapped` (`wrapMinutesOfDay` then `minutesToTime`). Do not implement next-bookable as `slotUntilTime(start, occupancy + buffer)`. `23:00` + 90 → `00:30` (not `24:30`); slot generation may still break at 24h if callers skip the wrap                                                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/reservations/operating-hours.test.ts` → `slotUntilTime`, `nextBookableTime`                                                                                                                                                                                                           |
| Inclusive membership vs exclusive assignment               | Shared `segmentContainsTimeInclusive`; booking validation (`isTimeWithinSegments`) stays inclusive; exclusive segment ownership only in `assignSegmentForTime` (later `opens_at` wins)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tests/unit/reservations/operating-hours.test.ts` → `isTimeWithinSegments`, `assignSegmentForTime`                                                                                                                                                                                                |
| Source-structure widget contract                           | `readFileSync` + regex on `reservation-widget.tsx`; assert helper imports (`groupBookableSlots`, `slotUntilTime`), until-badge `slotUntilTime(time, occupancyDurationMinutes)` (no buffer / no `expected_minutes`), `data-testid` hooks (`slot-group`, `slot-card`, `until`, `reserve`), and absence of time `<Select>`                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/unit/reservation-widget/segment-groups.test.ts`, `tests/unit/reservation-widget/chrome-i18n.test.ts`                                                                                                                                                                                       |
| Staff singleton settings (RSC → client island)             | Admin RSC fetches singleton via `React.cache()` `loadRestaurantBookingSettings`; pass clamped `initialSlotInterval` / `initialOccupancyDuration` / `initialSafetyBuffer` into the floor island; persist via `upsertRestaurantSetting`. Guest occupancy getter is a distinct export (`getGuestOccupancyDurationMinutes`), not the staff-gated `getOccupancyDurationMinutes`                                                                                                                                                                                                                                                                                                                                                                          | `tests/unit/floor/slot-interval.test.ts` → "floor plan exposes and persists restaurant-wide slot interval"; `tests/unit/floor/occupancy-settings.test.ts` → "floor plan exposes and persists occupancy duration and safety buffer (default 15)"                                                   |
| Declared-dep `existsSync` for TS2307                       | `package.json` + lockfile declare the dep; `existsSync(node_modules/<pkg>/package.json)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `tests/unit/dev-toolchain/typecheck-toolchain.test.ts` → "swr is installed so TypeScript can resolve the module"                                                                                                                                                                                  |
| Literal `items: MenuItemRow[]` on hook return              | Source-regex allows `useSWR<… items: T[]>` **or** function return `items: T[]`; a named SWR generic still needs a literal `items: T[]` after `): {`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tests/unit/site/chefs-picks-types.test.ts` → "homepage chefs picks map callback is MenuItemRow"                                                                                                                                                                                                  |
| Grid fields inline on `FloorTableView`                     | Literals `id: string`, `x: number`, `y: number` inside the `FloorTableView` body; `AssignableTable` stays x/y-free                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/floor/layout.test.ts` → "floor table view type includes id x y for spreadOverlappingTables"                                                                                                                                                                                           |
| Pin Next `ignoreBuildErrors` via UTF-8                     | `readFileSync` UTF-8 of `next.config.mjs` (do not import — it loads `next-intl/plugin`); reject truthy `ignoreBuildErrors` literals; omit-or-false when Next default is already fail-closed. Same class as G-L1 `--max-warnings 0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tests/unit/dev-toolchain/typecheck-toolchain.test.ts` → "next config does not ignore TypeScript build errors"                                                                                                                                                                                    |
| Pin ESLint `globalIgnores` via `isPathIgnored`             | `new ESLint({ cwd: repoRoot }).isPathIgnored(path)` — true for generated trees, false for a live config file; do not scrape `pnpm lint` stdout                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `tests/unit/dev-toolchain/lint-toolchain.test.ts` → "ignores gitignored supabase CLI temp and branches trees"                                                                                                                                                                                     |
| Pin `scripts.lint` `--max-warnings 0`                      | `package.json` `scripts.lint` substring `--max-warnings 0` (fail-on-warnings); then delete unused imports the old warning-tolerant gate hid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/dev-toolchain/lint-toolchain.test.ts` → "lint script passes --max-warnings 0 to eslint"                                                                                                                                                                                               |
| Pin unused-disable as error via ESLint Node API            | `new ESLint({ cwd: repoRoot }).lintText` on a known unused-disable snippet; find `ruleId === null` + `/Unused eslint-disable directive/` → `severity === 2`. Do not regex `eslint.config.mjs` for `reportUnusedDisableDirectives`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/dev-toolchain/lint-toolchain.test.ts` → "errors (not warns) on an unused eslint-disable directive"                                                                                                                                                                                    |
| Pin Prettier as a repo tool                                | `devDependencies.prettier` + `existsSync(node_modules/prettier)`; `scripts.format` / `scripts.format:check`; `.prettierrc.json` `semi: false`; `.prettierignore` snapshot trees                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `tests/unit/dev-toolchain/format-toolchain.test.ts` → "prettier is installed with format and format:check scripts"                                                                                                                                                                                |
| Postgres TIME vs generated `HH:MM`                         | Occupancy cover `normalizeTime`s reservation `time` before comparing to generated slots (Postgres `TIME` serializes as `19:00:00`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/reservations/available-slots.test.ts` → "occupancy-window cover counting"; `tests/unit/reservations/operating-hours.test.ts` → `normalizeTime`                                                                                                                                        |
| One occupying-status set                                   | Export `ACTIVE_RESERVATION_STATUSES` (`confirmed` / `seated`) and use it for both PostgREST `.in("status")` and the JS cover skip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tests/unit/reservations/available-slots.test.ts` → "releases 20:30 when a full-floor 19:00 hold is completed, cancelled, or no_show, while seated still occupies"                                                                                                                                |
| Dual-file privilege lock                                   | Identical `GRANT SELECT` / `REVOKE INSERT, UPDATE, DELETE` strings for `anon`/`authenticated` on `operating_windows` plus `GRANT ALL ON TABLE <t> TO service_role` in baseline and `20260825140000_operating_windows_privilege.sql` for `operating_windows` and the early-baseline siblings `blocked_dates`, `reservations`, `menu_items` (fold GRANT ALL into that file when remote `schema_migrations` has not recorded it; byte-identical GRANT ALL strings; siblings keep authenticated `FOR ALL`); `DROP POLICY IF EXISTS` on `operating_windows` only (keep the name; never `CREATE`); `NOTIFY pgrst` on dated forwards. Catalog anon/authenticated privileges are the three-file RES-PRIV / PUBLIC-READ-PRIV lock — see Integration recipes. | `tests/unit/scheduling/schema.test.ts` → "operating_windows grants are select-only for anon and authenticated"; "operating_windows grants ALL to service_role in baseline and privilege forward files"; "early-baseline tables grant ALL to service_role in baseline and privilege forward files" |
| Anti-seed occupancy + page source pin                      | Fixture 7 seated / 9 total / 0 available (cannot echo `TABLES` seed 3/10/4). `readFileSync` on `app/admin/page.tsx` forbids `\bTABLES\b` and requires `getFloorSnapshot(` + `countFloorOccupancy` — inversion-proof without importing the Server Component.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/floor/dashboard-occupancy.test.ts`                                                                                                                                                                                                                                                    |
| Live-inventory RSC prop-drill + source pin                 | Server Component fetches live inventory (`getTables()`), `dynamic = "force-dynamic"`, drills into a client picker (`<PosTerminal tables={…}>`). `readFileSync` on `app/pos/page.tsx` + `components/staff/pos-terminal.tsx` forbids `\bTABLES\b` and requires `getTables(`, `tables.map(`, `tables[0]?.label` — inversion-proof without a render harness.                                                                                                                                                                                                                                                                                                                                                                                            | `tests/unit/floor/pos-table-picker.test.ts` → "POS table picker lists live getTables() tables, not the TABLES seed"                                                                                                                                                                               |
| Occupying-window exclusive end (wrap → 24:00)              | Same-date BW-9 exclusivity: take exclusive end from `nextBookableTime`, then map a wrapped clock (`endMin <= startMin`) to 24:00. Do not compare wrapped `HH:MM` strings across midnight.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tests/unit/reservations/auto-assign.test.ts` → "reuses a table when occupying windows do not overlap and refuses when they do"                                                                                                                                                                   |
| Extra `.eq` after `.in()` in memory                        | Staff-action unit mocks that only implement `select → eq → in → order` (`auto-assign-action.test.ts`) cannot take an extra `.eq("table_label")` after `.in()`. Keep that chain; apply extra predicates in memory with a `// minimality:` note.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `tests/unit/reservations/assign-table.test.ts` → "rejects assigning a table whose occupying window overlaps another confirmed or seated reservation"                                                                                                                                              |
| Seat-fit before overlap (null unassign)                    | Select `seats` with the table row and `party_size` with the reservation, then `if (table && table.seats < party_size)` before overlap. Unassign is a null table, not a flag. Leave dropdown filtering to its own criterion.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/reservations/assign-table.test.ts` → "rejects assigning a table with fewer seats than party size"                                                                                                                                                                                     |
| Client-safe dropdown inventory helper                      | Extract a client-safe leaf helper (`import type` only — do not import `auto-assign`) for dropdown inventory (available+fit, current-label exception) so the manager source-string pin can name the function without mounting the component.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/reservations/selectable-tables.test.ts` → "omits undersize tables except the reservation current label"                                                                                                                                                                               |
| Staff list fail-closed Result                              | Convert a staff list from `return []` to `{ rows: [], error }` using the sibling stable strings (`Unauthorized.` / `Could not load reservations.`). Omit `error` on success (do not set `error: undefined`). Typecheck-only caller unwraps are expected until a UI criterion owns `.error`.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/reservations/get-by-date.test.ts` → "does not present auth or query failure as a successful empty list"                                                                                                                                                                               |
| Client-safe empty-copy helper                              | Error-then-flag branching. Keep unused snapshot fields on the input type so the object-literal pin typechecks without the helper re-deriving filters. Pin the manager with the helper name plus Result unwrap (`.reservations` / `.error`), not an inlined filter sentence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/reservations/list-empty-copy.test.ts` → "distinguishes load error, empty date, and filter-empty copy"                                                                                                                                                                                 |
| Named Tailwind `lg` Sheet open-gate                        | Export `shouldOpenMobileInspector(width)` keyed off `FLOOR_LG_MIN_PX` (1024). Callers gate `setMobileInspectorOpen(true)`; never treat `lg:hidden` on `SheetContent` as mobile-only while the overlay still mounts. Resize closes the Sheet at `lg+`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tests/unit/floor/schema.test.ts` → "selecting a table at lg does not open the mobile inspector Sheet"                                                                                                                                                                                            |
| Source-regex lock-in of the true open-path                 | Pin `setMobileInspectorOpen(true)` beside `shouldOpenMobileInspector` in `selectTable` so a boolean-assign cannot silently delete below-lg open-on-select.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/unit/floor/schema.test.ts` → "selecting a table below lg opens the bottom Sheet inspector"                                                                                                                                                                                                 |
| Bookability inventory vs live-floor `available`            | After `toAssignableTables`, drop only `out_of_service`. `canSeatPartyOnTables` ignores live `seated` / `reserved` / `cleaning`. `selectBestTable` still filters `status === "available"`. Occupancy/buffer trailing defaults match `planAutoAssignments` (90+15). Shared private `pickBestFitTable`.                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tests/unit/reservations/table-fit.test.ts`                                                                                                                                                                                                                                                       |
| Hermetic omitted-URL env fallback pin                      | Save prior `process.env.NEXT_PUBLIC_SUPABASE_URL`; stub inside `try`. In `finally`, `delete` the key if it was previously `undefined` (assigning `undefined` does not remove it in Node). Omitted/`undefined` follows env; an explicit URL wins.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `tests/unit/scheduling/hours-mutation-target.test.ts` → "omitted url follows env; explicit url wins even when env is local"                                                                                                                                                                       |
| Staff `restaurant_settings` persist Result                 | `requireStaffUser` then `{ error: "Unauthorized." }` (never throw); service-role upsert `{ id: 1, …patch, updated_at }`. Unit-test with hoisted `requireStaffUser` + `from`/`upsert` mocks and `objectContaining` so extra columns stay free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `tests/unit/marketing/review-email-settings.test.ts` → "staff persist toggle copy maps URL and delay; unauthenticated writes return Unauthorized"                                                                                                                                                 |
| Keep `isHttpsUrl` in `lib/`                                | `new URL(value).protocol === "https:"` in try/catch. Never export the helper from a `"use server"` file (`app/actions/marketing.ts` imports `@/lib/marketing/https-url`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tests/unit/marketing/review-email-settings.test.ts`; `tests/unit/marketing/review-email-send.test.ts`                                                                                                                                                                                            |
| Injected `now` + invalid delay defaults to 24              | Inject `now` into the send job; stamp `completed_at` on the status write; due = `now >= completed_at + delayHours` with `Number.isFinite` fail-closed; invalid delay (NaN, out of 0–72) defaults to 24 rather than bound-clamping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/marketing/review-email-send.test.ts` → delay cases; `app/actions/reservations.ts` `transitionReservationStatus`                                                                                                                                                                       |
| PV-6 claim-before-send `sent_at` CAS                       | Claim with `.update({ sent_at }).eq(reservation_id).is("sent_at", null).select("reservation_id")` BEFORE `mailer.send()`; treat `error` or empty `data` as a lost claim (do not read `count` unless `{ count: "exact" }`). Release `sent_at: null` on mailer throw, then rethrow. Never `.eq(col, null)` for a NULL CAS. Do not stamp after send — overlapping `processDue` would double-send.                                                                                                                                                                                                                                                                                                                                                      | `tests/unit/marketing/review-email-send.test.ts` → "overlapping processDue invocations send at most once for the same reservation"; "second successful send never happens; settings edit does not resend; failure allows retry"                                                                   |
| Cron GET fail-closed empty secret                          | Empty/unset `CRON_SECRET` or `authorization !== \`Bearer ${secret}\``is 401 so an unset env never matches`Bearer undefined`. Unit-test unauthenticated invocations must not call the processor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `tests/unit/marketing/review-email-job-auth.test.ts` → "unauthenticated send-job invocation does not send"                                                                                                                                                                                        |
| Pin `server-only` processor, not `"use server"`            | Source-read `/^import ["']server-only["']/` plus no file-level `"use server"` on the cron-Bearer processor. Global `vi.mock("server-only", () => ({}))` in `tests/unit/setup.ts` so Vitest can load the module. Never put `"use server"` on a Bearer-authenticated job — that would expose an unauthenticated Server Action.                                                                                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/marketing/review-email-job-auth.test.ts` → "processor module imports server-only and is not a use-server file"                                                                                                                                                                        |
| Required email, optional phone                             | `EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/` after trim; reject with `!email \|\| !EMAIL_RE`. Invert phone to `if (phone && !PHONE_RE)` so blank/whitespace skips the pattern. Widget Email `required`; Phone is not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tests/unit/reservations/validation.test.ts` → "rejects a missing email"; `tests/unit/reservation-widget/guest-email.test.ts` → "confirm passes email and Email is required while Phone is not"                                                                                                   |
| Local `&`-first `escapeHtml`; throwing mailer stub         | Keep a local `&`-first `escapeHtml` in each mailer module. BW-14 html is `[name, date, time, party, conf].map(escapeHtml).join(" ")`. Inject `{ send }` inside a `server-only` helper (not `"use server"`); catch at the Server Action so a throw still returns `{ confCode }`. Type the stub `{ send: (payload?: unknown) => unknown }`.                                                                                                                                                                                                                                                                                                                                                                                                           | `tests/unit/marketing/booking-confirmation.test.ts` → "confirmation html includes escaped name date time party and conf_code"; `tests/unit/reservations/create-reservation.test.ts` → "calls confirmation send after a successful insert with the guest email and conf_code"                      |
| Base UI Switch + hidden `name`                             | Base UI `Switch` is a `span` (id/name do not give a reliable labeled checkbox). Persist with controlled `checked` + hidden `name` (`"on"` vs `""`) and name the control with `aria-labelledby`, not `htmlFor` / `aria-label`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `tests/unit/marketing/marketing-page.test.ts`                                                                                                                                                                                                                                                     |
| Staff claim vs session-only gate                           | Hoist a `getUser` mock on `@/lib/supabase/server` `createClient` and drive `requireStaffUser` with null, `user_metadata.role: "staff"` spoof, and `app_metadata.role: "staff"` so a session-only gate cannot vacuous-pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/unit/auth/require-staff.test.ts` → "returns null for authenticated users without app_metadata.role staff"                                                                                                                                                                                  |
| One clone, pathname from session                           | Collapse dual staff-route redirects into one `nextUrl.clone()`; pathname `user ? "/" : "/auth/login"`; treat `isStaffUser(null)` as the unauthenticated case. Hoist `getUser` on `@supabase/ssr` `createServerClient`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tests/unit/auth/staff-proxy.test.ts` → "authenticated non-staff cannot open staff paths"                                                                                                                                                                                                         |
| Client-safe staff landing source pin                       | After `signInWithPassword`, gate `/admin` with `isStaffUser` from `@/lib/supabase/is-staff-user` (not `require-staff`). `readFileSync` the login page so a client form does not need a render harness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tests/unit/auth/login-staff-gate.test.ts` → "login does not send non-staff sessions to /admin"                                                                                                                                                                                                   |
| Table-scoped TOML `enable_signup`                          | Pin `[auth]` and `[auth.email] enable_signup` via table-scoped TOML reads so an `[auth.sms] enable_signup = false` cannot vacuous-pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/unit/auth/signup-disabled.test.ts` → "local Auth and email signup flags are false"                                                                                                                                                                                                         |
| JSON.parse seed `raw_app_meta_data`                        | Pin GoTrue seed `raw_app_meta_data` by `JSON.parse` of the staff `auth.users` VALUES cell (not a file-wide `"role": "staff"` grep) so a comment or `raw_user_meta_data` cannot vacuous-pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/auth/seed-staff-claim.test.ts` → "seed staff user raw_app_meta_data includes role staff"                                                                                                                                                                                              |
| Characterization test for already-shipped behavior         | Write the Red `it()` against the spec even when source already implements it; report GREEN honestly (do not weaken the assertion to force a fail). Sibling file already covers `/admin`/`/auth`/`/api` the same way.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tests/unit/i18n/middleware-scope.test.ts` → "pos and kds are excluded from localization"                                                                                                                                                                                                         |

## Integration recipes

| Scenario                                 | Helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Reference test                                                                                                                                                                                                                             |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Hours RPC complete-replace, local-only   | Snapshot `operating_windows` in `beforeAll`. Restore with filtered **TABLE** `insert` of snapshot rows, then `delete` leftover ids (never `replace_operating_windows` — that RPC wipes the week; insert-first so a failed restore cannot empty hours). Skip rewrite when sorted ids match. Call `assertIsolatedHoursMutationTarget()` (`lib/scheduling/hours-mutation-target.ts`) at the start of both `beforeAll` and `afterAll` — restore is itself a mutating write, and Vitest still runs `afterAll` after a failed `beforeAll`; the guard fails closed against the shared linked project instead of skipping. Test target is local Supabase, not the linked remote (baseline already defines the RPC).                                             | `tests/integration/scheduling/replace-operating-windows.integ.test.ts`                                                                                                                                                                     |
| Complete-replace multiset pin            | After the RPC call, `select` **all** rows unfiltered and assert `toHaveLength(p_windows.length)` (no leftover/duplicate rows survive the replace). Then, for each expected `p_windows` entry, consume a match via `findIndex` + `splice` on the fetched rows (comparing `day_of_week`, `is_closed`, `label`, `sort_order`, and `normalizeTime`'d times), and assert the remaining unmatched array is empty. Reusable recipe for pinning "replace produced exactly this multiset, no dupes/no extras" RPC behavior.                                                                                                                                                                                                                                      | `tests/integration/scheduling/replace-operating-windows.integ.test.ts`                                                                                                                                                                     |
| Local-isolated Data API DML denial       | docker `GRANT SELECT, INSERT, UPDATE, DELETE` onto postgres Dxtm defaults to reproduce the linked-remote privilege surface, then pin authenticated insert error plus update/delete non-persist; keep `DROP POLICY IF EXISTS` without `CREATE` so `db reset` still strips a restored FOR ALL.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `tests/integration/scheduling/replace-operating-windows.integ.test.ts` → "authenticated Data API cannot insert, update, or delete operating_windows"                                                                                       |
| Three-file catalog privilege lock        | Two-line `-- REAZED-###: ACRONYM` comment inline above GRANT/REVOKE in baseline, `20260825140000_operating_windows_privilege.sql`, and `20260827160000_public_catalog_privileges.sql` (name the table when a second catalog table shares the acronym); DROP then GRANT/REVOKE; reservations `GRANT INSERT` + `REVOKE SELECT, UPDATE, DELETE` + `DROP POLICY IF EXISTS "Allow public read reservations"` (no `CREATE`; no `GRANT SELECT ON TABLE reservations`); `blocked_dates`/`menu_items` `GRANT SELECT` + `REVOKE INSERT, UPDATE, DELETE`; live Data API plus SQL-substring assertions; pin STRICT integration to `127.0.0.1` keys from `npx supabase status` when `.env.local` is the linked remote. Do not docker-GRANT overlay for this surface. | `tests/integration/reservations/public-privileges.integ.test.ts`                                                                                                                                                                           |
| Last-writer occupancy SQL (elapsed TIME) | Occupancy + table-fit of `validate_reservation_availability` byte-identical in baseline, `20260818162000_operating_hour_segments.sql`, `20260827180000_occupancy_duration_buffer.sql`, and `20260828121224_table_fit_availability.sql`. Table-fit after cover-count. Date-scoped `pg_advisory_xact_lock(305, days-since-epoch)` after cover-count, before table-fit reads (classid 305 = REAZED-305; date, not slot). Window is same-date elapsed `TIME` (`NEW.time::TIME - r.time::TIME < (occupancy+buffer) * interval`), never `TIME + interval`. `SECURITY DEFINER`. P0001 `Booking denied: This time is fully booked.` Live INSERT via `createReservation` (mock `next/cache`).                                                                    | `tests/integration/reservations/occupancy-window.integ.test.ts`; `tests/integration/reservations/table-fit.integ.test.ts`; `atomic-booking.integ.test.ts` last compatible unit                                                             |
| Assignment-feasible occupancy seeds      | Occupancy-window holds: one occupying reservation per table, `party_size = seats` (not a single full-floor party). Sequential table-fit: second overlapping party of 8 vs held 8-top. Concurrent atomic-booking: last compatible unit race.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/integration/reservations/occupancy-window.integ.test.ts`; `tests/integration/reservations/table-fit.integ.test.ts`; `atomic-booking.integ.test.ts` → "serializes concurrent bookings so the last compatible unit is only sold once" |
| Nullable PII on an insert-only table     | Put the column on `CREATE TABLE` and `ALTER TABLE … ADD COLUMN IF NOT EXISTS` with an idempotency comment; do not add `GRANT SELECT`. Prove with service-role insert + anon `select` 42501/PGRST301 (empty rows, payload has no guest email).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tests/integration/reservations/review-email-pii.integ.test.ts` → "anon cannot SELECT guest email on reservations"                                                                                                                         |

Add a row when `tdd-refactor` reports a `Reusable pattern:` worth keeping.

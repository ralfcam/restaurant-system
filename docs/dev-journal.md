# Dev journal

Chronological notes when a `.cursor/plans/*.plan.md` completes or a milestone ships.

- **Restaurant Link template + empty default logo** (2026-08-18) — Rebrand from äkta; `menu-catalog` rename; no bundled `/images/logo.png`; `BrandMark` null until staff upload.
- **G-T1 typecheck fix** (`g-t1_typecheck_fix_47606513`) (2026-08-25) — `pnpm typecheck` exits 0; declared `swr` on disk; Chef’s picks `MenuItemRow`; `FloorTableView` includes `id`/`x`/`y`.
- **OH-SAVE** (2026-08-25) — `20260818162000_operating_hour_segments` recorded on linked remote `tilcqrudqxznnpepxjqq` (not a full `db push`); staff save via `replace_operating_windows`.
- **OH-SAVE isolation + complete-replace pin** (`pr35_coderabbit_oh_save`) (2026-08-25) — CodeRabbit fix on PR 35: mutating integration coverage now fails closed to local Supabase only (`assertIsolatedHoursMutationTarget`, `lib/scheduling/hours-mutation-target.ts`) instead of running against the deployed linked project; strengthened the RPC pin to assert an exact complete-replace multiset; deployed PGRST202 stays manual-UAT.
- **G-L1 temp ignore** (`g-l1_temp_ignore_da077232`) (2026-08-25) — `eslint.config.mjs` `globalIgnores` lists gitignored `supabase/.temp/**` and `supabase/.branches/**` so `pnpm lint` does not lint generated Supabase CLI output.
- **G-L1 max-warnings** (`reazed-272_lint_max_warnings`) (2026-08-25) — `package.json` `scripts.lint` is `eslint . --max-warnings 0` so warnings fail `pnpm lint`; unused `dirname` import dropped from `.cursor/checks/harness-lint.mjs`.
- **G-F1 Prettier** (2026-08-25) — `prettier` in `devDependencies`; `pnpm format` / `pnpm format:check`; `.prettierrc.json` `semi: false`; snapshot trees prettierignored. Whole-tree `format:check` is still red until a dedicated format pass.
- **OH-PRIV** (`reazed-290_hours_privilege`) (2026-08-25) — SELECT-only `anon`/`authenticated` table privileges on `operating_windows`; drop authenticated `FOR ALL`; forward `20260825140000_operating_windows_privilege.sql` for already-baselined remotes (manual-UAT apply; do not `db push`).
- **OH-PRIV service_role GRANT** (`reazed-297_service-role_grant_5bde11c5`) (2026-08-25) — `GRANT ALL ON TABLE operating_windows TO service_role` in baseline and still-unapplied `20260825140000_operating_windows_privilege.sql` (no third dated file; `20260825140000` not recorded on `tilcqrudqxznnpepxjqq`). Anon/authenticated stay SELECT-only. Staff Save stays service_role RPC.
- **EARLY-PRIV sibling GRANT** (`reazed-297_sibling_grants_097b94f3`) (2026-08-27) — `GRANT ALL ON TABLE blocked_dates TO service_role`, same for `reservations` and `menu_items`, in baseline and still-unapplied `20260825140000_operating_windows_privilege.sql` (no third dated file). Authenticated `FOR ALL` on those tables kept. Distinct from `reazed-297_service-role_grant_5bde11c5` (`operating_windows` only).
- **RES-PRIV / PUBLIC-READ-PRIV** (`reazed-308_privileges_be131e14`) (2026-08-27) — Reservations insert-only (no public SELECT); `blocked_dates`/`menu_items` GRANT SELECT + REVOKE DML for anon/authenticated in baseline, `20260825140000`, and forward `20260827160000_public_catalog_privileges.sql` (apply when `20260825140000` is already recorded; do not `db push`).
- **REAZED-309 occupancy buffer** (`reazed-309_occupancy_buffer_1862f023`) (2026-08-27) — Restaurant-wide occupancy duration (default 90) + staff-manageable safety buffer (default 15) drive next-bookable; until-badge occupancy only; trigger last-writer `20260827180000_occupancy_duration_buffer.sql` (manual-UAT apply on `tilcqrudqxznnpepxjqq`; do not `db push`).
- **REAZED-306 live occupancy** (`reazed-306_live_occupancy_44f250a2`) (2026-08-28) — `/admin` Dashboard Floor occupancy / Service is live / Floor status count `getFloorSnapshot` tables via `countFloorOccupancy`, not `lib/data.ts` `TABLES` (FP-11).
- **REAZED-300 staff reservations** (`reazed-300-staff-reservations_3fd7b670.plan`) (2026-08-28) — Occupancy-window auto-assign taken-set (FP-3); manual assign fit/overlap write + dropdown (FP-5); fail-closed staff date list (STAFF-LIST).
- **G-T1 ignoreBuildErrors** (`reazed-278_ignorebuilderrors_7ba76e17`) (2026-08-28) — `next.config.mjs` omits `typescript` (Next default fail-closed); UTF-8 pin rejects truthy `ignoreBuildErrors` literals so `next build` cannot skip TypeScript errors.
- **REAZED-304 floor select blur** (`reazed-304_floor_blur_320155e0`) (2026-08-28) — `/admin/floor` selection opens the mobile Sheet only below `lg`; side inspector at lg+; resize closes the Sheet (FP-12).
- **REAZED-305 table-fit** (`reazed-305_table-fit_ebc5d205`) (2026-08-28) — Guest slots and confirm-path table-fit (BW-12); `canSeatPartyOnTables`; last-writer `20260828121224_table_fit_availability.sql` + date-scoped `pg_advisory_xact_lock(305, epoch-days)` (do not `db push`). Guest INSERT still does not write `table_label`.
- **OH-SAVE hermetic unit pin** (`oh-save_pin_hermetic_2088c96a`) (2026-08-28) — Unit pin of `assertIsolatedHoursMutationTarget` stubs `NEXT_PUBLIC_SUPABASE_URL` so omitted/`undefined` follows env and an explicit URL wins; helper `url ?? process.env.NEXT_PUBLIC_SUPABASE_URL` unchanged.
- **Post-visit review email** (`post-visit_review_tdd_ac1962e1`) (2026-08-30) — `/admin/marketing` write-only settings (`saveReviewEmailSettings`); `processDueReviewEmails` + Bearer `CRON_SECRET` GET (`app/api/cron/review-email/route.ts`, throwing mailer stub, no `vercel.json` cron); complete stamps `completed_at` and inserts `review_email_sends` (those objects not in schema yet except nullable `reservations.email`; RES-PRIV unchanged).
- **REAZED-310 staff authorization** (`reazed-310_staff_auth_a6b9be91.plan`) (2026-08-30) — Staff is JWT `app_metadata.role === "staff"` (`isStaffUser`); `requireStaffUser` and `/admin`/`/pos`/`/kds` proxy reject non-staff; login gates `/admin`; local `[auth]`/`[auth.email] enable_signup = false`; seed `admin@test.local` `raw_app_meta_data.role = staff`. Hosted signup (SA-6) is manual-UAT.
- **Mailing automations triple** (`mailing_automations_triple_2347e1d5.plan`) (2026-08-30) — PV-9 `import "server-only"` on `lib/marketing/review-email.ts` (not `"use server"`); PV-6 claim-before-send CAS on `sent_at`; BW-13 required guest email / optional phone; AC-4/BW-14 `sendBookingConfirmation` after insert (throwing stub; mailer failure does not withhold `conf_code`). Live provider remains manual-UAT.
- **G-L1 unused-disable** (`reazed-274_lint_disable_directive_e91a4bc7`) (2026-09-01) — `eslint.config.mjs` `linterOptions.reportUnusedDisableDirectives: "error"`; unit pin via ESLint `lintText` on a known unused-disable snippet (`severity === 2`).
- **REAZED-316 POS table picker** (`reazed-316_pos_table_picker_28440958`) (2026-09-01) — `/pos` Table Select lists live `getTables()` rows via `app/pos/page.tsx` → `PosTerminal` `tables` prop, not `lib/data.ts` `TABLES` (FP-13).
- **REAZED-280 POS/KDS locale scope** (`reazed-280_pos_kds_locale_scope_4d8f1c92`) (2026-09-01) — Spec + regression test document `/pos`/`/kds` locale exclusion (already in `LOCALE_EXCLUDED_PREFIXES`).
- **REAZED-326 super-admin permission** (`reazed-326_super_admin_permission_model`) (2026-09-02) — JWT `super_admin` (`isSuperAdminUser` / `requireSuperAdminUser`); `isStaffUser` is `"staff"` OR super-admin; branding/booking-config writes, `updateRestaurantContactInfo`, and `saveReviewEmailSettings` are super-admin-only; seed `superadmin@test.local` (`2222…`) plus existing staff `admin@test.local`.
- **REAZED-326 seed users email** (`seed_users_email_f5f7f0e6.plan.md`) (2026-09-02) — FIX: pin seed `auth.users.email` to the identity login addresses (`admin@test.local` / `superadmin@test.local`, pairwise distinct); empty string is a real unique key on GoTrue `users_email_partial_key`.
- **REAZED-327/328 proxy + workspace root** (`reazed-327-328_middleware_proxy_workspace_root_a77d6de1`) (2026-09-02) — FIX: pin `next.config.mjs` `turbopack.root` + `outputFileTracingRoot` to the config file's directory (G-W1); Next 16 request boundary is root `proxy.ts` / `export async function proxy` (G-P1); `lib/supabase/proxy.ts` stays the session helper.
- **Staff chrome + POS inventory** (`ux_staffchrome_pos_batch_9c4a1b`) (2026-09-02) — FIX: SC-4a `shouldUseLightNavText` / homepage `overDarkBackground={hasHero}`; HP-1 hero `grid … md:grid-cols-2`; SA-10 `isSuperAdmin` chrome; FP-14 `servers` + `getServers()`; FP-13 empty-table Select disable.
- **REAZED-296 chef's picks service** (`reazed-296_chefs_picks_service`) (2026-09-03) — FIX: `setChefsPicksEnabled` upserts `restaurant_settings` via `createServiceClient()` after `requireStaffUser` (BC-1); sibling menu catalog DML stays on cookie `createClient`.
- **REAZED-311/312 POS catalog + orders** (`reazed-311-312_pos-catalog-persistence_7c3f9a1d`) (2026-09-03) — FIX: `/pos` `getMenuItems()` → `PosTerminal` `items` (no `MENU_ITEMS`); `createKitchenOrder` live `menu_items` re-check; `orders`/`order_items` in baseline (staff-only RLS; KDS polls).
- **RES-61 guest i18n followups** (`res-61_guest_i18n_followups`) (2026-09-08) — Guest header chrome from `nav` catalogs (AC-11); next-intl `Link` on guest hrefs and `NextLink` on `/admin` (AC-12); `isActiveNavPath` for locale-stripped Menu active state (AC-13); mobile sheet closes on language switch (AC-14); dual `LanguageSwitcher` regions (AC-15); `resolveDocumentLang` drives root `<html lang>` (AC-16). AC-9 remains manual-UAT.
- **RES-59 Chef's picks SSR + avatar** (`res-59_chefs_picks_ssr_avatar`) (2026-09-09) — FIX: homepage RSC seeds `useChefsPicks` via `getHomepageChefsPicks` (HP-2); featured-dish avatar `{name.trim()[0] ?? "?"}` (HP-3).
- **HTML lang client sync + pnpm pin** (`html_lang_pnpm_pin_70d248bc`) (2026-09-09) — FIX: `DocumentLangSync` writes `document.documentElement.lang` after client navigation (AC-17); G-O1 lock-in `packageManager` equals `pnpm@12.3.4`.
- **RES-45 review-email schema + hourly cron** (`res-45_review_email_schema_cron_7f3a91c2`) (2026-09-09) — FIX: baseline `review_email_*` settings columns, `review_email_sends` (service-role-only), `reservations.completed_at`; `createReviewEmailMailer()` on `GET /api/cron/review-email`; hourly timer is Supabase Edge Function `review-email` + `pg_cron` (no Vercel `crons` — Hobby rejects hourly). Live inbox remains manual-UAT.
- **RES-39 pin reservation integ local** (`res-39_pin_reservation_integ_local_a7c3e1f2`) (2026-09-09) — FIX: RES-ISO unit glob-scan plus `assertIsolatedHoursMutationTarget()` as the first statement of `beforeAll`/`afterEach` in every `tests/integration/reservations/*.integ.test.ts`. Reuse hours helper; no `createServiceClient` guard; no application source.
- **Isolation follow-up pins** (`res_iso_followup_pins_b8e4c2a1`) (2026-09-09) — FIX: RES-ISO scan requires a zero-arg helper call and rejects an explicit URL; PV-ISO pins marketing integ write hooks including `beforeEach`; ORD-ISO adds a pin-only `beforeAll` plus `afterEach` on POS integ. No application source.
- **RES-50 locale mw session cookies** (`res-50_locale_mw_session_cookies_a3f1c8e2`) (2026-09-09) — FIX: `proxy.ts` copies `getAll()` cookie options onto the intl response (AC-18); `resolveLocaleRoutingDecision` is segment-bounded (AC-19).
- **RES-42 sibling privilege lock** (`res-42_sibling_privilege_lock_6f2a8c1d`) (2026-09-10) — FIX: drop authenticated `FOR ALL` on sibling catalog/floor/orders tables; `REVOKE ALL` then only the public grant; staff Data API uses `createServiceClient` after `requireStaffUser`; linked conformance is operator-owned reset (RES42-M1).
- **RES-37 service client boundary** (`res-37_service_boundary_ffc7ec3c`) (2026-09-10) — FIX: `lib/supabase/service.ts` first-line `import "server-only"` (SA-11); factory URL/key/auth options unchanged; unit source pin plus `vi.mock("server-only", () => ({}))` in unit and integration setup.
- **RES-41 guest note cap** (`res-41_guest_note_cap_49176a07`) (2026-09-10) — FIX: 240-character OH-NOTE-SAVE reject in `validateOperatingDays`; staff guest-note input `maxLength={MAX_GUEST_NOTE_LENGTH}`; widget inherits, does not truncate.
- **Guest reservation INSERT privileges** (`guest-reservation-insert-privileges_587ea4c9`) (2026-09-10) — FIX: guest `GRANT INSERT` on `reservations` is column-scoped (`guest_name`, `party_size`, `date`, `time`, `phone`, `email`, `notes`, `conf_code`) in baseline, `20260825140000`, and `20260827160000`; table-wide guest INSERT forbidden. Linked remote `tilcqrudqxznnpepxjqq` already records `20260827160000` (RES-PRIV-REMOTE deferred/manual; this run did not mutate remote).
- **RES-47 grant hardening** (`res_47_grant_hardening_a330bfed`) (2026-09-10) — FIX: after every `CREATE OR REPLACE` of `validate_reservation_availability()`, `REVOKE ALL` from PUBLIC and from anon, authenticated in the four function-defining migrations; local reset denies guest EXECUTE (RES-TRIGGER-EXEC). Linked re-apply of `20260828121224` on `tilcqrudqxznnpepxjqq` is deferred manual-UAT (RES-TRIGGER-EXEC-LINKED).
- **G-L1 unit timeout** (`eslint_timeout_hardening_f994f816`) (2026-09-10) — FIX: `vitest.unit.config.ts` `test.testTimeout: 15_000` for dependency-heavy ESLint Node API probes; unit pin plus existing `ESLint.isPathIgnored` coverage (G-L1 C4).
- **RES-46 hours RPC search_path** (`res_46_search_path_cc45bd3e`) (2026-09-10) — FIX: `replace_operating_windows` stays `SECURITY INVOKER` with exact `SET search_path = ''` and `public.operating_windows` writes in baseline and `20260818162000`; local reset pins `pg_proc.proconfig` `search_path=""`. Linked replay of recorded `20260818162000` on `tilcqrudqxznnpepxjqq` is deferred manual-UAT (OH-SAVE-PATH-LINKED; do not repair history).
- **RES-49 fail-closed blocked dates** (`res_49_fail_closed_8d0cc815`) (2026-09-10) — FIX: `isDateBlocked` / `getBlockedDatesInMonth` / `getBlockedDatesInRange` log `blocked_dates` SELECT errors and throw `Error("Could not load blocked dates.")`; success empty/data and anon queries unchanged. Caller recovery UX out of scope (BD-READ-FAIL).
- **RES-TRIGGER-EXEC AUTHLESS** (`res_trigger_authless_2132ecb2`) (2026-09-11) — FIX: named trigger-EXECUTE catalog `it()` lives outside `describe.skipIf(!authEnvReady)` with its own local `beforeAll` pin; missing Docker/`psql` fails closed; sibling matrix stays auth-gated (RES-TRIGGER-EXEC-AUTHLESS).
- **Reservation code uniqueness** (`reservation-code-uniqueness_c4d50986`) (2026-09-11) — FIX: AC-4 CONF-CODE-UNIQUE; baseline `CREATE UNIQUE INDEX IF NOT EXISTS reservations_conf_code_uidx ON public.reservations (conf_code)` immediately after the reservations table create; no dated forward. Guest INSERT allowlist still includes `conf_code`; `createReservation` 23505 retry unchanged.
- **RES-93 reservation analytics** (`analytics_tdd_wave_44867fc0`) (2026-09-12) — FEATURE: staff `getReservationAnalytics` (RA-1–RA-10); `/admin/analytics` StaffShell chrome (does not load the reader); `StaffShell` Service nav `href: "/admin/analytics"`; aggregators in `lib/analytics/report.ts`; no guest SELECT GRANT.
- **RES-91 event inquiries** (`res-91_event_inquiries_b25c4399`) (2026-09-13) — FEATURE: staff `event_inquiries` ledger (EI-1–EI-9); `/admin/inquiries` list-only chrome (open+contacted); SIB-PRIV sibling after `servers` GRANT; no convert/confirm; occupancy stays on `reservations`.
- **G-CR3 commit must-fixes** (`g-cr3_commit_must-fixes_cc22b981`) (2026-09-13) — FIX: adapter re-GETs the PR after pagination and fails `head_changed`; `wrong_base_head` for disallowed shapes including `main → staging`; parsed `.coderabbit.yaml` `reviews.auto_review.drafts === true` (comment-only `# drafts: true` does not). G-CR2 empty `reviewedFiles` stays `scope_mismatch`. Command `/ready-merge-release`.
- **G-CR2 empty reviewedFiles** (`g-cr2_empty_files_387e8b97`) (2026-09-13) — FIX: dual absent/empty review_context file lists and `complete.reviewedFiles` mint the work-order dirty set then continue finding-count/waivers; non-empty mismatch and mixed empty-versus-listed fail-closed; out-of-work-order dual-omission findings fail `scope_mismatch`. CLI pin remains `0.7.6`.
- **G-CR2 omitted-context complete mint** (`g-cr2_complete_mint_d26b357a`) (2026-09-13) — FIX: omitted/empty review_context aliases + matching `complete.reviewedFiles` mint that complete list; dual-omission still mints the work-order; inspect every non-empty alias (never `a || b || c`); finding paths ⊆ dirty on every completed stream. G-CR3 `fetchSnapshot` stays exported with in-body `/pulls/${number}` re-GET. CLI pin remains `0.7.6`.
- **G-CR2/G-CR3 null ≠ omit + nonempty head** (`null_omit_empty_head_8dc143b1`) (2026-09-14) — FIX: `isOmittedOrEmptyFileList` treats only missing (`undefined`) or `[]` as omit/empty; JSON `null` on a review_context alias is `scope_mismatch`; `complete.reviewedFiles: null` is `reviewed_files_mismatch`. `isAllowedReadyPrShape` requires nonempty trimmed `base`/`head`; missing/`""`/whitespace is `wrong_base_head`. CLI pin remains `0.7.6`.
- **G-CR2 4G locks** (`g-cr2_4g_locks_bb8ea9b1`) (2026-09-14) — FIX: G-CR2 defers the CLI pin to G-CR1. Exact `evaluateAgentStream` reasons: empty aliases skipped; any non-empty alias mismatch is `scope_mismatch` even when another listed alias matches (`matching reviewedFiles does not hide a mismatching files alias`); mixed listed-context + empty complete stays `reviewed_files_mismatch`.
- **G-CR3 US allow-list** (`g-cr3_us_allow-list_d9699f1f`) (2026-09-14) — FIX: G-CR3 is a US allow-list (`coderabbitai` / App `347564`); non-US CodeRabbit-shaped identity on reviews/checks/threads is `wrong_bot` before unresolved threads; `eu_bot_activity` retired.
- **G-CR3 4G unresolved** (`g-cr3_4g_unresolved_d74a8f3f`) (2026-09-14) — FIX: issue/inline comment authors are `wrong_bot`; `/ready-merge-release` re-reads `headRefOid` immediately before `gh pr ready` and runs `gh pr ready --undo` on post-ready drift; Critical/Major/unknown still `/sdd-to-tdd`.
- **G-CR2 opener rewrite** (`g-cr2_opener_rewrite_46cf360e`) (2026-09-14) — FIX: G-CR2 §8 opener is not an unqualified fail-closed on missing `complete.reviewedFiles` equality; matching complete-list mint and dual-omission mint remain; `evaluateAgentStream` unchanged. New unit `it`: "G-CR2 opener does not fail-closed on missing complete.reviewedFiles equality".
- **G-CR3 4G majors** (`g-cr3_4g_majors_da59fa0c`) (2026-09-14) — FIX: just-ready `--undo` (already-ready drift STOP with no `--undo`); `isUsApp` is App ID `347564` only; both `gh` spawnSync `timeout: 15_000`; independent `remote-staging-*-base.json` fixtures (adapter unchanged).
- **G-CR2 4G soften** (`g-cr2_4g_soften_761b7912`) (2026-09-14) — FIX: recorded-not-blocking after `applyWaivers` (spec-structure on `docs/specs/**`, 98fd7524-class runbook mint, already-ledger `--undo` without `<n>`); `pull_changed` after pagination re-GET; workflow `on.pull_request.types` includes `edited`; ready-merge Step 2 `/sdd-to-tdd`/`/capture` use `<local-ref>` only.
- **Advisory CodeRabbit 4G** (`advisory_coderabbit_4g_af126342`) (2026-09-14) — G-CR2 now requires a local review attempt but treats findings and vendor/tool/JSONL/scope outcomes as audit-only `clean` / `findings` / `unavailable` receipts. Deterministic work-order, secret, unrelated-dirt, empty-surface, and changed-byte failures stay hard. Commit authorization is receipt-independent; G-CR3 remains fail-closed.
- **RES-70 admin/guest menu tabs** (`res-70_admin_menu_tabs_c3a91f2b`) (2026-09-15) — FIX: live `menus` table (PUBLIC-READ-PRIV; seeded `midi`, `soir`, `boissons`, `blanc`, `rouge`); staff `getMenuTabs` / `createMenuTab` / `renameMenuTab` / `reorderMenuTabs` via `requireStaffUser` + `createServiceClient`; guest `getPublicMenuTabs` via anon; `/admin/menu` dish Select and `/[locale]/menu` tab chrome read live rows (empty tabs stay visible). POS picker tabs and staff filter/section chrome stay compiled `MENUS`.
- **RES-70 CR majors** (`res-70_cr_majors_9f4c2a71`) (2026-09-15) — FIX: `reorderMenuTabs` is one `reorder_menu_tabs` RPC (`SET search_path = ''`; not SECURITY DEFINER; `GRANT EXECUTE` to `service_role` only); dated-forward `20260915180000_menus_bootstrap.sql` plus companion `CREATE TABLE IF NOT EXISTS menus` before GRANT; PRE-SATISFIED STRICT pin of `menus-privileges.integ.test.ts` (no second throw / no `skipIf` rewrite).
- **RES-70 CR remain** (`res-70_cr_remain_b5c93e14`) (2026-09-15) — FIX: dated `20260915180000_menus_bootstrap.sql` inserts five MT-3 ids `ON CONFLICT (id) DO NOTHING`; companions `20260825140000` / `20260827160000` ENABLE RLS + public-read / service_role policies before GRANT; `vitest.integration.config.ts` `test.env.RESTAURANT_INTEGRATION_STRICT` is `"true"` (throw stays in `tests/integration/setup.ts`).
- **RES-70 CR threads** (`res-70_cr_threads_e8a1c4d2`) (2026-09-15) — FIX: every `CREATE TABLE IF NOT EXISTS menus` file inserts the five MT-3 ids (`midi`, `soir`, `boissons`, `blanc`, `rouge`) `ON CONFLICT (id) DO NOTHING` after its menus GRANT/REVOKE trio (baseline, `20260825140000`, `20260827160000`); dated `20260915180000` already had this INSERT. No new dated migration.
- **RES-104 guest profiles** (`res-104_guest_profiles_f8c2e1a0`) (2026-09-16) — FEATURE: staff ficha identity `normalizeGuestEmail`; `getGuestProfile` / `updateGuestProfilePii` via `requireStaffUser` + `createServiceClient`; reservation-row `guestProfileHref` `Link`. `/admin/customers/[email]` chrome still prints `profile.error ?? email` (GP-4/GP-5/GP-12 display, GP-10 submit not on page). No `guests` table.
- **RES-104 CR majors** (`res-104_cr_majors_b3e8a1c2`) (2026-09-16) — FIX: live read `.eq("email_normalized", normalizeGuestEmail(email))` on generated STORED `lower(btrim(email))` + `reservations_email_normalized_idx`; mutator stays `.eq("email")`. Ficha Save calls `updateGuestProfilePii({ email, guest_name, phone })`; chrome interpolates `profile.email` / `profile.notes`, exclusive empty ternary (`No reservations — not found.` vs history), `const fichaHref` + ternary `Link`.
- **RES-104 CR mutator** (`res-104_cr_mutator_d4b2a9c1`) (2026-09-16) — FIX: `updateGuestProfilePii` `.eq("email_normalized", normalizeGuestEmail(input.email))` (GP-10 write of GP-2 group); write-site comment labeled GP-10.
